# SIGP — NEG Ingeniería S.A.S. BIC

Sistema Integral de Gestión de Proyectos. Este archivo es el contexto operativo que cualquier instancia de Claude debe leer antes de tocar código en este repositorio.

---

## 0. Contexto SST heredado — leer primero

Este archivo aplica **encima** del `CLAUDE.md` del directorio padre (`APLICACION SST/CLAUDE.md`), que documenta el panel web SST original y el proyecto Firebase compartido. Cuando trabajes en `sst-panel-web/src/modules/sigp/`, ambos archivos rigen simultáneamente: el padre define la base compartida (panel SST, app Flutter, marca, restricciones absolutas); este define la ampliación SIGP.

**Reglas heredadas del padre que este archivo mantiene:**

1. **NO tocar la app Flutter** (`neg_sst_app/`). Su código y sus formularios están en producción y son estables. Cualquier cambio en colecciones compartidas debe ser retrocompatible.
2. **Cloud Functions y reglas Firestore requieren aprobación explícita del usuario antes de deploy a producción**. Diseño y prueba en emulador local son libres; producción requiere OK del humano en cada caso.
3. **Estrategia de subagentes por modelo**: Haiku para exploración y lectura, Sonnet para escribir código de complejidad media (default), Opus para arquitectura, revisión previa a producción y decisiones críticas. Nunca Haiku para código que va a producción directamente.
4. **La colección se llama `formularios`, no `registros`** — en versiones tempranas de la documentación se usó el nombre viejo. El nombre real en Firestore es `formularios`. Las visitas técnicas del SIGP también usarán esta colección con un `tipo` nuevo.
5. **`normalizarDoc()` es el helper canónico para leer documentos de Firestore** cuando la app Flutter y el panel usan nombres de campo distintos. Cualquier lectura de `formularios` en el SIGP debe usarlo.
6. **Los roles heredados del panel SST son tres**: `tecnico` (solo app móvil), `sst` (panel web), `admin` (total). El SIGP amplía este esquema — ver capítulo 9 de este archivo.
7. **La marca completa está definida en el CLAUDE.md padre**. Este archivo referencia lo mínimo necesario para el SIGP; ante cualquier duda de color, tipografía o logo, consultar el padre.

**Precedencia ante conflicto:** el CLAUDE.md padre gana en dominios que ya existen (SST, marca, restricciones absolutas). Este gana solo en el dominio nuevo del SIGP. Ante duda, pregunta al usuario.

---

## 1. Qué es este repositorio

El SIGP es el sistema operativo de la operación de proyectos de NEG Ingeniería (Colombia, S.A.S. BIC, NIT 900.975.870-1). NEG presta servicios de ingeniería eléctrica, civil, telecomunicaciones y arquitectónica, y opera hoy con procesos formales del SGI (ISO 9001 + 14001 + 45001) pero con la operación de proyectos viviendo en WhatsApp, Telegram, correo y Excel.

El SIGP cierra ese gap: hace que la operación de proyectos ocurra dentro del sistema, y que la evidencia ISO se genere como **subproducto automático** de operar, no como carga documental adicional.

---

## 2. Filosofía rectora — no negociable

**La evidencia ISO se genera automáticamente como consecuencia de operar, no como carga documental adicional.**

Cada vez que estés diseñando o codificando algo, pregúntate: "¿qué evidencia ISO deja este hito?". Si la respuesta es "hay que llenar un formato adicional", el diseño está mal. La evidencia debe ser el subproducto natural de que alguien hizo su trabajo dentro del sistema.

Corolarios:
- No hay pantallas cuyo único propósito sea "llenar formato para auditoría".
- No se piden datos dos veces.
- Los timestamps, autores y estados son parte del dato, no adjuntos.
- Los PDFs se generan del sistema, nunca se suben a mano si el sistema podía producirlos.

---

## 3. Stack técnico

- **Backend**: Firebase (proyecto `neg-sst-app`) — Firestore + Auth + Storage (SDK v10 modular)
- **Panel web**: React 18 + Vite 5 + Tailwind CSS 3, TypeScript 100%, desplegado en Vercel
- **App móvil**: Flutter (Android primero, iOS después), publicada en Play Store como app SST extendida
- **Routing**: react-router-dom v6
- **Gráficos**: recharts v3
- **Excel**: xlsx (SheetJS) — ya está en el stack para F1 (parser de LPU)
- **PDFs**: `pdf-lib` — **NO instalado hoy**. Se agrega en F0 como preparación para generación de PDFs en el panel (Tarea 1.4.5 del plan F1). Los PDFs actuales del panel SST vienen de la app Flutter (`pdf_url`); el panel solo los muestra.
- **Firebase Storage**: SDK disponible pero **`getStorage()` no está inicializado en `firebase/config.ts`**. Se extiende en F0 antes de arrancar F1 (necesario para el Excel del LPU en Tarea 1.1.4).
- **Testing**: **no hay infraestructura de test instalada hoy**. Vitest + Firebase Emulator Suite se configuran como parte de F0.
- **Notificaciones**: Firebase Cloud Messaging + correo transaccional
- **CI/CD**: GitHub Actions → Vercel (web) y App Distribution (Flutter)

### Repositorios relacionados

- `sst-panel-web/` — panel administrativo (donde vive el SIGP web). ~60% del código base es reutilizable (auth, `components/shared/`, `useFirestore`, patrón Firestore, marca).
- `neg_sst_app/` — app móvil SST + módulo de proyectos para contratistas y personal de campo.

**Regla dura**: nada en el SIGP puede romper la funcionalidad SST en producción. Los cambios que tocan colecciones compartidas (`users`, `formularios`, `obras`, `contratistas`, `notificaciones`) requieren pruebas de regresión SST.

---

## 4. Marca y estilo visual

La marca completa está definida en el CLAUDE.md padre. Aquí solo el resumen operativo:

- **Verde primario**: `#628E3A` = `brand-600` en Tailwind
- **Verde oscuro (estados activos)**: `#4F7330` = `brand-700`
- **Acento lima**: `#D7DA33` = `accent`. Usar con moderación — muy saturado, no para texto pequeño.
- **Neutros**: negro `#000000`, gris oscuro `#454545`, fondo gris claro `#EFF1F4`. Tarjetas blancas con borde 1px.
- **Semánticos SST** (estados de revisión, no de marca): pendiente ámbar, aprobado emerald, rechazado red.
- **Tipografía UI**: Montserrat (`font-sans` / `font-display`) en pesos 400, 500, 600, 700. Weights 300 e italic prohibidos en UI general.
- **Slogan**: solo Lato italic (`font-slogan`), reservado para *"Ingeniería que cambia el mundo"*.
- **Logo símbolo**: `public/logo-neg.png` (turbina Pelton).
- **Logo completo**: `public/logo-neg-full.png` (con sello Sociedades BIC).
- **Tono**: profesional, directo, técnico, sin adornos. Nunca infantil, nunca corporativista genérico.
- **UI inspiración**: Linear/Vercel. Sidebar claro (blanco, ítem activo verde). Prohibido: glassmorphism, modo oscuro forzado, cualquier color azul.

⚠️ **Nota histórica**: sesiones tempranas asumieron paleta azul (`blue-700/900`) que quedó obsoleta tras el rebrand del 28-may-2026. Cualquier azul en el código es error, no legado válido.

En PDFs generados (cotizaciones, actas, informes) usar `Plantilla_de_documento.docx` como referencia estructural.

---

## 5. Restricciones críticas

1. **Cero borrado físico**. Todo es soft-delete con `estado: archivado` o `activo: false`. La trazabilidad ISO no perdona registros perdidos.
2. **Versionado de documentos**. Cotizaciones, actas y liquidaciones se versionan (`version: int`); nunca se sobrescriben.
3. **Snapshots de datos volátiles**. Los precios del LPU se copian en la cotización al crearla — no se referencian por FK. Si el LPU cambia, las cotizaciones históricas quedan intactas.
4. **Consecutivos transaccionales**. Formatos con año en el ID (`OFR-2026-001`, `PRY-2026-001`, `FAC-2026-001`) generados con transacciones Firestore atómicas — nunca en cliente.
5. **No tocar el SST en producción**. La app Flutter (`neg_sst_app/`) no se modifica bajo ninguna circunstancia. Cualquier cambio a colecciones compartidas (`users`, `formularios`, `obras`, `contratistas`, `notificaciones`) debe ser retrocompatible y probarse primero en Firebase Emulator Suite contra el flujo SST completo.
6. **Cloud Functions y reglas Firestore — aprobación explícita para producción**. Diseñar, escribir y probar en Firebase Emulator Suite es libre. Deploy a producción requiere OK explícito del usuario en cada caso. No hay excepciones "porque es un cambio menor".
7. **Autenticación obligatoria**. Ninguna colección tiene reglas `allow read/write: if true`. Todo pasa por `request.auth.uid` y rol.
8. **PDFs con hash**. Cada PDF generado guarda su hash SHA-256 en el documento correspondiente — evidencia de integridad.
9. **Consulta, no gestión — patrón para módulos externos**. El módulo de proyectos **consume estados** de otros módulos (SGI, administrativa) pero **no los escribe**. Los dominios externos hoy incluyen: habilitación SST del contratista (escrita por SGI) y ciclo financiero de anticipos, pagos y facturación (escrita por gerencia administrativa). Ver reglas 7.2 y 7.6.

---

## 6. Modelo de datos (resumen por dominio)

El modelo completo está en el ERD (`ERD_SIGP_NEG.pdf`). Aquí el mapa mental por dominio:

### Dominio comercial (Fase 1)
- `clientes` — con `tiene_lpu` y `tipo_documento_cierre` (acta o liquidación)
- `lpu_clientes` — lista de precios unitarios (Excel del cliente parseado a items[])
- `solicitudes` — con `origen` (info_suficiente / insuficiencia / solicitud_cliente)
- `visitas_tecnicas` — opcional, cuelga de la solicitud
- `cotizaciones` — con `modo: lpu|libre`, `version`, `items_snapshot`

### Dominio proyecto (núcleo, Fase 2)
- `proyectos` — con `valor_venta_vigente` (actualizable por ajustes)
- `asignaciones` — 1:1 con proyecto, referencia al contratista con criterio y puntaje
- `preliquidaciones` — 1:1 con proyecto, valor_venta / valor_contratista / utilidad / anticipo_pct
- `contratistas` — compartido con SST; `estado_habilitacion` es escrito por SGI, leído por SIGP

### Dominio ejecución (Fase 3)
- `avances` — fotos por actividad, subidas por el contratista
- `ajustes_obra` — tipificados: `interno` (solo utilidad) o `adicional_cliente` (afecta venta y liquidación)
- `documentos_cierre` — colección genérica que soporta actas de entrega Y liquidaciones finales, tipificada por `tipo_documento`

### Dominio financiero (Fase 4) — gestionado por administrativa
- `facturas` — ligadas a `documento_cierre_id`, no directo al proyecto
- `pagos` — con `tipo`: `anticipo_contratista`, `pago_cliente`, `saldo_contratista`. El saldo no se registra sin un pago del cliente previo.

Ambas colecciones son **escritas por el módulo de gerencia administrativa** (aún por construir). El módulo de proyectos solo las **lee** para mostrar el estado financiero del proyecto. Ver regla 7.6.

### Dominio calidad y aprendizaje (transversal)
- `no_conformidades` — garantías, rechazos, hallazgos de auditoría
- `evaluaciones` — puntaje del contratista al cierre del proyecto
- `satisfaccion` — encuesta corta al cliente al cierre

### Infraestructura
- `consecutivos` — contadores atómicos por tipo y año
- `notificaciones` — reutilizada del sistema SST

### Remote Config
- **`sigp_f1_enabled`** (boolean, default `false`) controla la visibilidad del módulo SIGP en el panel. Se lee vía el hook `useFeatureFlag` (`src/hooks/useFeatureFlag.ts`). Ajustable desde la consola de Firebase sin necesidad de deploy.

---

## 7. Reglas de negocio críticas

Estas reglas están codificadas en el modelo y deben respetarse en la UI y en Cloud Functions:

### 7.1. Snapshot del LPU

Cuando se crea una cotización con `modo = 'lpu'`, los ítems seleccionados y sus precios se **copian** dentro del documento de la cotización (`items_snapshot: array`). Nunca se referencian por FK al LPU en tiempo de lectura. Razón: el LPU del cliente puede cambiar y las cotizaciones históricas deben conservar los precios pactados.

```typescript
// Correcto
cotizacion.items_snapshot = lpu.items
  .filter(item => seleccionadas.includes(item.codigo))
  .map(item => ({ ...item, cantidad: cantidades[item.codigo] }));

// Incorrecto - NO leer LPU al mostrar cotización histórica
cotizacion.items = lpu.items.filter(...);  // rompe historia
```

### 7.2. Gate SST — consulta, no gestión

El módulo de proyectos **nunca** modifica `contratistas.estado_habilitacion`. Solo lo lee. Quien lo actualiza es el módulo transversal de Gestión Integral (SGI). Antes de permitir que un proyecto pase a estado `en_ejecucion`, se valida que el contratista asignado tenga `estado_habilitacion === 'habilitado'`.

Si el contratista no está habilitado, la UI muestra un mensaje claro: "El contratista X está pendiente de habilitación por parte de Gestión Integral" — no ofrece "solucionarlo desde aquí".

### 7.3. Ciclo financiero de dos momentos

El pago al contratista se divide siempre en dos eventos:
1. **Anticipo** (50-65%) al inicio de obra, tras aprobación de la preliquidación.
2. **Saldo** solo después de que exista un `pago` de tipo `pago_cliente` registrado.

Regla en Cloud Function (o en reglas Firestore) que rechaza la creación de un `pago` de tipo `saldo_contratista` si no existe un `pago_cliente` previo para el mismo proyecto.

```typescript
// Pseudocódigo de validación
if (nuevo_pago.tipo === 'saldo_contratista') {
  const hay_pago_cliente = await db.collection('pagos')
    .where('proyecto_id', '==', nuevo_pago.proyecto_id)
    .where('tipo', '==', 'pago_cliente')
    .limit(1).get();
  if (hay_pago_cliente.empty) {
    throw new Error('No se puede pagar saldo al contratista sin pago del cliente registrado');
  }
}
```

### 7.4. Ajustes en obra — tipificación estricta

Todo `ajuste_obra` tiene `tipo`:
- `interno`: error de NEG. `impacto_venta = 0`, `impacto_costo` puede ser positivo o negativo. **No se traslada al cliente.**
- `adicional_cliente`: adición aprobada por el cliente. `impacto_venta` y `impacto_costo` ambos actualizados. Requiere evidencia de aprobación (correo, comunicación).

Al registrar el ajuste, `proyecto.valor_venta_vigente` se actualiza automáticamente. La preliquidación original queda intacta; los ajustes son entidad separada.

### 7.5. Documento de cierre — genérico

`documentos_cierre` soporta dos tipos:
- `acta_entrega` — firmada por el coordinador o supervisor del cliente. Default.
- `liquidacion_final` — firmada por el representante legal del cliente. Ej: Ingemec.

El tipo por default para un cliente viene de `clientes.tipo_documento_cierre`, pero se puede sobreescribir en el proyecto si es necesario. Las facturas se emiten contra el `documento_cierre_id`, nunca directamente contra el proyecto.

### 7.6. Pagos y facturación — consulta, no gestión

El módulo de proyectos **nunca** escribe en `pagos` ni en `facturas`. Estas colecciones son escritas exclusivamente por el módulo de gerencia administrativa (aún por construir). El módulo de proyectos solo las lee para:

- Mostrar el estado financiero del proyecto (anticipo registrado / factura emitida / pago recibido / saldo liquidado).
- Bloquear transiciones de estado que dependen de eventos financieros (ej: no marcar `en_ejecucion` sin `anticipo_contratista` registrado; no marcar `cerrado` sin `saldo_contratista` liquidado).
- Alimentar el dashboard de proyectos con indicadores financieros.

Este es el mismo patrón que se usa para la habilitación SST (regla 7.2): el módulo de proyectos consume el estado, no lo produce.

**Implicaciones para F4**: cuando se llegue a la Fase 4 (Cierre financiero), la decisión de diseño será construir el mini-módulo administrativo como parte de F4, o esperar a que administrativa tenga su propio módulo independiente. La segunda opción es más limpia arquitectónicamente pero deja F4 dependiente de otro entregable. Esa decisión se toma al llegar a F4, no ahora.

**Reglas Firestore** para `pagos` y `facturas`:
- Read: roles `gerencia_administrativa`, `gerencia_general`, `director_proyectos`, `operacion_comercial`, `admin`, `gestion_integral`.
- Write: solo roles `gerencia_administrativa` y `admin`. Ningún componente del módulo de proyectos genera escrituras a estas colecciones.

Cuando el módulo de proyectos necesite mostrar información financiera, siempre usa lecturas de `pagos` y `facturas` filtradas por `proyecto_id`. Nunca calcula ni sintetiza estos datos.

---

## 8. Consecutivos

Formato: `PREFIJO-YYYY-NNN` (padding con 3 dígitos, se extiende a 4 si supera 999 en un año).

| Prefijo | Entidad | Uso |
|---|---|---|
| `SOL` | solicitudes | Registro de solicitud entrante |
| `OFR` | cotizaciones | Cotización enviada al cliente |
| `PRY` | proyectos | Proyecto creado al aprobar cotización |
| `ACT` | documentos_cierre (tipo acta) | Acta de entrega |
| `LIQ` | documentos_cierre (tipo liquidación) | Liquidación final estilo Ingemec |
| `FAC` | facturas | Factura emitida |
| `NC` | no_conformidades | No conformidad registrada |

Los consecutivos se generan con transacciones atómicas Firestore sobre la colección `consecutivos/{prefijo}_{año}`:

```typescript
// Cloud Function o cliente autorizado
const generarConsecutivo = async (prefijo: string, año: number): Promise<string> => {
  return await db.runTransaction(async (tx) => {
    const ref = db.doc(`consecutivos/${prefijo}_${año}`);
    const snap = await tx.get(ref);
    const siguiente = (snap.data()?.ultimo ?? 0) + 1;
    tx.set(ref, { ultimo: siguiente, actualizado: FieldValue.serverTimestamp() });
    return `${prefijo}-${año}-${String(siguiente).padStart(3, '0')}`;
  });
};
```

**Nunca** generar consecutivos con `Date.now()`, hashes o UUIDs. La secuencialidad es evidencia ISO.

---

## 9. Roles y permisos

Los roles viven en `users.rol` y las reglas Firestore los usan para autorizar.

**Roles heredados del panel SST original** (siguen vigentes para el subsistema SST):

- `tecnico` — solo app móvil, sube formularios
- `sst` — panel web, revisa formularios, aprueba/rechaza
- `admin` — total en el panel

**Roles nuevos que introduce el SIGP** (coexisten con los anteriores en la misma colección `users`):

| Rol | Permisos clave |
|---|---|
| `admin` | Todo. Solo para configuración e infraestructura. |
| `gerencia_general` | Aprobación de proyectos > umbral, revisión por la dirección, dashboards ejecutivos |
| `gerencia_administrativa` | Preliquidaciones, pagos, facturación, presupuestos |
| `operacion_comercial` | Solicitudes, cotizaciones, aprobaciones comerciales |
| `director_proyectos` | Proyectos, asignaciones, avances, actas |
| `auxiliar_proyectos` | Apoyo operativo a proyectos: creación de obras y seguimiento (cargo real actual, p. ej. Paula Moreno) |
| `residente_sst` | Visitas técnicas (para levantamiento), lectura de proyectos |
| `gestion_integral` | Habilitación de contratistas, no conformidades, auditorías, indicadores |
| `contratista` | Sube avances por actividad, ve sus proyectos asignados |
| `cliente_final` (futuro) | Ve informes y actas de sus proyectos, aprueba entregas |

### Modelo de acceso por área (Opción C — validado 05-jul-2026)

El acceso a cada panel se determina por **área funcional**, no por listas de roles por-ruta. Dos helpers en `types/sigp/roles.ts` son la fuente única de verdad:

- **`accesoSST(rol)`** → `true` para `sst`, `admin`, `gerencia_general`, `gestion_integral`, `residente_sst`.
- **`accesoSIGP(rol)`** → `true` para `admin`, `gerencia_general`, `gerencia_administrativa`, `operacion_comercial`, `director_proyectos`, `auxiliar_proyectos`, `gestion_integral`.

Cada helper consume su array exportado (`ROLES_CON_ACCESO_SST` / `ROLES_CON_ACCESO_SIGP`), que también alimenta la protección de rutas en `App.tsx` (`<ProtectedRoute rolesPermitidos={ROLES_CON_ACCESO_SIGP}>`) y el filtro del Sidebar. Consecuencia: **cualquier rol con acceso SIGP ve todas las vistas SIGP**; la granularidad fina (qué acciones puede ejecutar dentro de una vista) se maneja **dentro de cada página**, no en el ruteo ni el sidebar.

> `operacion_comercial` (antes `gerencia_comercial`) es un rol **operativo**, no gerencial — refleja la multifuncionalidad real de NEG (personas cumplen funciones comerciales sin cargo comercial dedicado).

Regla base en Firestore rules:

```javascript
function rol() {
  return get(/databases/$(database)/documents/users/$(request.auth.uid)).data.rol;
}
function esRol(r) {
  return request.auth != null && rol() == r;
}
function algunRol(rs) {
  return request.auth != null && rs.hasAny([rol()]);
}
```

### Mapeo de roles heredados a SIGP

Los usuarios existentes con rol `sst` o `admin` en el panel SST tienen acceso al SIGP según su nuevo rol asignado. La migración **no es automática** — cada usuario existente se revisa y se le asigna el rol SIGP correspondiente en un batch de configuración. Un usuario puede tener un solo rol activo. Si necesita acceso a más de un dominio, se le da el rol de mayor privilegio que le corresponda.

Ejemplos de mapeo esperado:
- Usuario con rol `admin` (panel SST) que gestiona proyectos → migra a `director_proyectos` o `gerencia_general` según su función real.
- Usuario con rol `sst` que solo aprueba formularios → migra a `residente_sst`.
- Usuarios `tecnico` (que solo usan la app móvil) no requieren cambio hasta que se implemente el rol `contratista` en F3.

El batch de migración se define y se ejecuta como parte de F0.

---

## 10. Convenciones de código

### Nomenclatura
- **Colecciones y campos Firestore**: `snake_case` en español (`documentos_cierre`, `valor_venta_vigente`)
- **Componentes React**: `PascalCase` en español (`FormularioCotizacion`, `TablaProyectos`)
- **Hooks**: `useAlgo` en camelCase inglés (`useProyectos`, `useConsecutivo`)
- **Tipos TypeScript**: `PascalCase` inglés (`Proyecto`, `Cotizacion`, `AjusteObra`) — cerca del dominio
- **Rutas**: kebab-case español (`/proyectos/nuevo`, `/cotizaciones/:id/editar`)

### Estructura del panel web

**Estructura real: plana, sin `modules/`.** Detectada en Fase 0.0 (03-jul-2026). El SIGP convive con el código SST existente usando **subcarpeta `sigp/`** dentro de cada carpeta de propósito único. No se reorganiza el código actual — Opción A confirmada.

```
sst-panel-web/src/
├── App.tsx, main.tsx, index.css
├── components/
│   ├── [componentes SST existentes]
│   ├── shared/           # librería UI interna: Modal, MultiSelect, TextField, etc.
│   ├── ProtectedRoute.tsx  # (nuevo en F0) reemplaza AdminRoute inline con genérico
│   └── sigp/             # todos los componentes SIGP nuevos
│       ├── clientes/
│       ├── lpu/
│       ├── solicitudes/
│       ├── cotizaciones/
│       ├── proyectos/
│       ├── ejecucion/
│       ├── cierre/
│       └── calidad/
├── pages/
│   ├── [páginas SST existentes]
│   └── sigp/             # páginas SIGP nuevas
├── contexts/             # AuthContext, NotificacionesContext (SST); nuevos contexts del SIGP acá
├── hooks/
│   ├── useFirestore, useModal, useObrasConRegistros (SST)
│   ├── useConsecutivo.ts (nuevo en F0)
│   └── sigp/             # hooks específicos del SIGP (opcional)
├── utils/                # vencimiento.ts (SST); utilidades SIGP acá con prefijo
├── types/
│   ├── formulario.ts     # fuente única SST: Formulario, normalizarDoc, TIPO_LABELS
│   └── sigp/             # tipos SIGP nuevos: Cliente.ts, Cotizacion.ts, etc.
└── firebase/
    ├── config.ts         # (extender en F0) agregar getStorage()
    └── consecutivos.ts   # (nuevo en F0) helper transaccional
```

**Reglas de convivencia:**

1. **Nada del código SST existente se mueve o se renombra en F0.** Se agrega, no se reorganiza.
2. **Componentes SIGP nuevos van dentro de `components/sigp/{subdominio}/`.** Nombre del componente en PascalCase español (`FormularioCotizacion.tsx`).
3. **Páginas SIGP nuevas van en `pages/sigp/`.** Rutas usan prefijo `/sigp/*` (por ejemplo `/sigp/cotizaciones`).
4. **Tipos SIGP nuevos van en `types/sigp/`.** Un archivo por entidad principal (`types/sigp/Cotizacion.ts`).
5. **Componentes compartidos entre SST y SIGP se mantienen en `components/shared/`.** No se duplica.
6. **Las reglas Firestore viven en `firestore.rules` en la raíz del proyecto** (no en `src/firebase/rules/`). Se editan directamente allí.
7. **Migración de `/obras` al SIGP (F0.5.b)**: el módulo Obras tiene ahora ubicación canónica en **`/sigp/obras`** (protegida por `ROLES_CON_ACCESO_SIGP`, visible en el Sidebar SIGP), porque los proyectos SIGP nacen del flujo comercial y las obras son su manifestación física. La ruta **`/obras`** original (admin-only, Sidebar SST) **se conserva** para retrocompatibilidad. Ambas renderizan el mismo componente `Obras.tsx` (no duplicado).

**Reorganización futura**: cuando F1 esté estable y haya cobertura de tests, se puede evaluar migrar a estructura `modules/sst/` + `modules/sigp/` como tarea dedicada. No en F0/F1.

### Tests

- Tests unitarios con Vitest para lógica pura (validaciones, cálculos de utilidad, generación de consecutivos).
- Tests de integración con Firebase Emulator Suite para reglas de seguridad.
- Cada regla de negocio del capítulo 7 debe tener al menos un test que verifique el caso feliz y uno que verifique el rechazo.
- Cobertura mínima objetivo: 70% en `modules/sigp/`.

### Convivencia de sesiones paralelas — worktrees (práctica adoptada 07-ago-2026)

**Cada carril/tarea activa trabaja en su PROPIO `git worktree`** — nunca dos
sesiones editando el mismo working tree. Origen de la regla: el incidente del
C3 (PR #65), donde una sesión paralela sobrescribió `firestore.rules` sin
commitear entre el E2E y el commit de otra, y el PR mergeado perdió sus
bloques de reglas (lo atrapó la pre-verificación del deploy).

Mecánica:
- `git worktree add ../wt-<carril> -b <rama-del-carril>` al arrancar el carril
  (un worktree por rama; las sesiones con aislamiento automático ya lo hacen
  bajo `.claude/worktrees/`).
- El working tree principal queda reservado para `main` (docs/bitácora,
  limpiezas, deploys).
- Antes de commitear reglas u otros archivos compartidos: verificar que el
  diff a commitear contenga EXACTAMENTE los bloques propios (no basta con
  ver el archivo "modified").
- `.claude/worktrees/**` está excluido de la suite de Vitest (no pertenece
  al repo lógico).

### Commits

Convención: `<área>: <acción imperativa>` en español.

Ejemplos:
- `cotizaciones: agrega selector de LPU con búsqueda`
- `consecutivos: transacción atómica para OFR`
- `docs: actualiza CLAUDE.md con regla de saldo contratista`

---

## 11. Cuándo consultar al humano

Claude Code debe **detenerse y preguntar** cuando:

1. La tarea implica **modificar colecciones compartidas con el sistema SST** (`users`, `contratistas`, `notificaciones`).
2. La tarea requiere **cambiar reglas de Firestore** en producción.
3. Un requerimiento no encaja con las reglas de negocio del capítulo 7 — probablemente hay que revisar la regla, no forzar la implementación.
4. La solución implica introducir una **nueva librería pesada** (>50 KB) o cambiar el stack.
5. Hay que decidir entre **dos estructuras de datos incompatibles** — la decisión afecta a futuro.
6. Se trata de un **flujo financiero** que no está explícitamente descrito.
7. El resultado impactaría **evidencia ISO histórica** ya emitida.

Frase útil: "Antes de proceder, quiero validar contigo esta decisión porque impacta [X]".

---

## 12. Roadmap F0 → F5

| Fase | Nombre | Alcance |
|---|---|---|
| F0 | Base | Instalar `pdf-lib`; extender `firebase/config.ts` con `getStorage()`; extraer `AdminRoute` inline a `components/ProtectedRoute.tsx` genérico; instalar Vitest y configurar Firebase Emulator Suite; crear estructura `sigp/` en `components/`, `pages/`, `types/`; implementar sistema de consecutivos con Cloud Function (requiere OK del usuario para deploy); layout SIGP y navegación; feature flag `sigp_f1_enabled` en Remote Config; batch de migración de roles heredados. |
| F1 | Comercial | LPU, solicitudes, visitas técnicas, cotizaciones, aprobación cliente. **(Ver `PLAN_FASE_1_SIGP.md`.)** |
| F2 | Planeación | Asignación de contratista con criterio, preliquidaciones, anticipo, gate SST |
| F3 | Ejecución | Módulo app Flutter para contratistas, avances por actividad, ajustes en obra, documentos de cierre |
| F4 | Cierre financiero | Facturación contra documento de cierre, pagos, saldo contratista, evaluación, satisfacción |
| F5 | Cierre ISO | Indicadores automatizados, tablero SGI, auditorías, revisión por la dirección |

### Estado de F0 — ✅ CERRADA (06-jul-2026)

F0 se cerró con el merge del **PR #1** (`sigp/f0-base` → `main`, commit de merge **`3f7673a`**) y el auto-deploy de Vercel a producción **verificado** (https://sst-panel-neg.vercel.app → HTTP 200). **`sigp_f1_enabled` sigue en `false`** → el SIGP está en `main` y desplegado, pero **invisible para todos los usuarios** hasta que F1 esté lista.

- 0.1 ✅ dependencias (pdf-lib, Vitest, Emulator Suite, Storage/Functions en `config.ts`)
- 0.2 ✅ `ProtectedRoute` genérico + estructura de carpetas `sigp/`
- 0.3 ✅ sistema de consecutivos (Cloud Function desplegada y validada en producción)
- 0.4 ✅ layout SIGP + rutas placeholder + sidebar
- 0.5 ✅ refactor de accesos (`accesoSST`/`accesoSIGP`) + Remote Config (`useFeatureFlag`) + migración Obras a `/sigp/obras` + migración de 5 usuarios + creación de 3 nuevos
- 0.6 ✅ reglas Firestore por rol (H-007) + **0.6.a-ter** + regresión SST + merge a `main`

**Sub-bloque 0.6.a-ter (NO estaba en la planificación original):** durante la validación funcional afloró que las reglas Firestore ya eran correctas, pero la UI mostraba elementos que los roles nuevos no debían ver ni usar. Se creó **`types/sigp/permisos.ts`** — fuente única de permisos de UI (helpers `veX(rol)` de visibilidad y `puedeX(rol)` de acción) que alimenta el gating del Sidebar, de las rutas (`ProtectedRoute`) y de los botones de cada página (aprobar/rechazar, gestión de obras, gestión + habilitación de contratistas). Además se desplegó una regla Firestore que permite a `gerencia_administrativa` un `update` limitado de `contratistas` **solo sobre el campo `estado`** (habilitar/deshabilitar), vía `diff().affectedKeys().hasOnly(['estado', 'fecha_actualizacion'])`.

**Seguimiento abierto post-F0:** H-001 (refinamiento opcional), H-004 (bump `firebase-functions` v6), H-005 (CI/CD de functions). Ver `HALLAZGOS_AUDITORIA.md`.

### Estado de F1 (Comercial) — ✅ FUNCIONALIDAD COMPLETA (11-jul-2026)

F1 se desarrolló por sub-iteraciones (ver `PLAN_FASE_1_SIGP.md`). Con el merge de F1.4-B el flujo comercial funciona de punta a punta: solicitud → visita técnica → cotización (LPU/catálogo/APU, con análisis económico interno) → envío con PDF → aprobación con evidencia. Queda **F1.5 (refinamientos comerciales)** como iteración de pulido antes de decidir la activación del flag.

- **1.1 ✅ Clientes y gestión de LPU** — cerrada 07-jul-2026 con el merge del **PR #2** (`sigp/f1.1-clientes-lpu` → `main`, commit de merge **`24d1fd3`**). Incluye: CRUD de clientes (contactos, condiciones IVA pleno/AIU); **importador asistido de LPU** (wizard con SheetJS: selección de hojas, mapeo de columnas con heurísticas, vista previa consolidada, escritura de ítems en batches ≤500 + Excel original a Storage + guardado del mapeo en el cliente); **detalle de LPU** con ítems agrupados por categoría/capítulo y **trazabilidad de versiones** (cadena `reemplaza_a`); reglas Firestore (`clientes`/`lpus`/`lpus/{id}/items`) y Storage (`lpus/**`) desplegadas; 19 tests de la lógica del importador. Validada contra producción con la LPU real de IHS (536 ítems). Ver **H-008** en `HALLAZGOS_AUDITORIA.md` (gate cross-service de Storage removido → `auth-only`; hardening por custom claims pendiente).
- **1.2 ✅ Solicitudes** — cerrada 07-jul-2026 con el merge del **PR #3** (`sigp/f1.2-solicitudes` → `main`, commit de merge **`d2ba9cf`**). Incluye: bandeja de entrada `/sigp/solicitudes` (lista + filtros + stats); **registro** con consecutivo `SOL-YYYY-NNN` vía la Cloud Function `generarConsecutivo` (con manejo de fallo tras consumir el número: se preserva y se reintenta sin quemar otro → evita huecos accidentales) + adjuntos a Storage; origen flexible `cliente_id` o `prospecto_nombre` (al menos uno); **detalle** con timeline del historial y **máquina de estados** (`TRANSICIONES`, motivo obligatorio al descartar, correcciones hacia atrás auditadas; `cotizada` reservada a F1.4, `descartada` terminal); reglas Firestore (`solicitudes`) y Storage (`solicitudes/**` auth-only) desplegadas; tests de la máquina de estados. Validada contra producción. `useConsecutivo` se hizo *lazy* (callable creado en la llamada, no al importar).
- **1.3 ✅ Visitas técnicas** — cerrada 07-jul-2026 con el merge del **PR #4** (`sigp/f1.3-visitas` → `main`, commit de merge **`3965693`**). Incluye: bandeja `/sigp/visitas` (filtros estado/tipo/cliente, badge NEG vs Contratista); **programar** visita (origen solicitud en `requiere_visita` con autocompletado, o cliente/prospecto; ejecutor NEG o contratista activo) con consecutivo `VIS-YYYY-NNN` (mismo manejo de fallo/reintento que SOL); **ejecución mobile-first** (checklist por tipo — estación base greenfield/rooftop —, hallazgos con foto de cámara, plano como foto del sketch, cantidades preliminares, guardado parcial que no pierde datos); **transición cruzada**: al marcar realizada, la solicitud vinculada en `requiere_visita` pasa a `lista_para_cotizar` con entrada de historial que referencia la visita. Cloud Function `generarConsecutivo` + prefijo `VIS` (desplegada); reglas Firestore (`visitas`) y Storage (`visitas/**` auth-only) desplegadas. Validada contra producción, incluida la captura mobile en celular real.
- **1.3-bis ✅ Shell del panel responsive** — cerrada 07-jul-2026 con el merge del **PR #5** (`sigp/f1.3-bis-shell-responsive` → `main`, commit de merge **`79ff369`**). En móvil (< lg, 1024px) el sidebar es un **cajón (drawer)** off-canvas con backdrop, abierto por la hamburguesa y cerrado al navegar o tocar fuera; contenido a ancho completo. En escritorio (≥ lg) el sidebar sigue en flujo y colapsable (rail) **sin cambios**. Nuevo `hooks/useMediaQuery.ts`; toda la lógica nueva detrás de `< lg` (regresión SST verificada). Validado en celular real. Se agregó `sst-panel-web/.claude/launch.json` con `--host` como infra de validación móvil por LAN.
- **1.4-A ✅ Cotizaciones (núcleo)** — cerrada 11-jul-2026 con el merge del **PR #6** (`sigp/f1.4a-cotizaciones` → `main`, commit de merge **`e5ef54a`**). Incluye: **modelo** — una cotización = un `COT-YYYY-NNN` con **versiones en subcolección** (`cotizaciones/{id}/versiones/{n}`, cada versión un snapshot completo con ítems inline, por el límite de 1MB/doc; el padre denormaliza total/estado/versión activa para la bandeja); **esquema tributario por cotización** (`iva_pleno` | `aiu` con IVA solo sobre la Utilidad — verificado contra el modelo Excel real), pre-sugerido desde las condiciones comerciales del cliente y siempre editable; `calcularTotales` con **redondeo a peso por componente** (CD/A/I/U/IVA/Total — el snapshot coincide con el PDF futuro); campo **`asunto`** (plantilla CM-FT-CT-19); estado `vencida` **derivado** en UI (enviada + validez vencida, no se almacena). **Bandeja** `/sigp/cotizaciones` (stats, filtros, columna Asunto). **Crear** desde solicitud `lista_para_cotizar` (transición automática a `cotizada` con historial referenciando el COT-; mismo manejo de fallo/reintento de consecutivo que SOL/VIS) o directa (cliente/prospecto). **Constructor de ítems**: buscador del LPU vigente (snapshot `origen:'lpu'` con trazabilidad `lpu_id`/`lpu_item_id`, **unidad heredada solo lectura**), ítems manuales, prellenado desde cantidades de la visita, capítulos con subtotales, descripción expandible a fila full-width, ítems en $0 advertidos sin bloquear (resalte ámbar), formato monetario es-CO. **Ciclo de estados**: `borrador→enviada→aprobada|rechazada` — enviar congela el snapshot (guarda antes) + `fecha_envio`; **aprobar exige evidencia adjunta** (validación dura, Storage `cotizaciones/**`); rechazar exige motivo; **nueva versión** desde enviada/rechazada/vencida (copia completa como borrador v+1, nunca desde aprobada); timeline del historial + comparación de versiones. **Desplegado con OK explícito**: reglas Firestore (`cotizaciones` + `versiones`), Storage (`cotizaciones/**` auth-only, H-008) y prefijo `COT` en `generarConsecutivo`. **H-009**: la LPU IHS se había importado con `unidad` vacía (encabezados en inglés, "UNIT OF MEASUREMENT"); heurística de mapeo extendida a plantillas bilingües + **backfill autorizado en prod (536/536 ítems, verificado)** — ver `HALLAZGOS_AUDITORIA.md`. Tests 26 → **35**. Validada contra producción (3 rondas de ajustes de Giovanny).
- **1.4-B ✅ Cotizaciones — APU, catálogo NEG, PDF y refinamientos** — cerrada 11-jul-2026 con el merge del **PR #7** (`sigp/f1.4b-apu-catalogo-pdf` → `main`, commit de merge **`9bde6f9`**). Incluye: **constructor APU** (formato canónico APU_CLARO_113: 5 secciones fijas — mano de obra, materiales, equipo, transporte, herramienta menor — insumos con `rendimiento` decimal fino, costo directo → margen [0,100) → precio sugerido; APU embebido como snapshot, `origen: 'apu'`); **análisis económico interno** (réplica DC-FT-CT-24, jamás en el PDF: `costo_directo`/`margen` por ítem — margen = % utilidad sobre el precio, factor 0,9 = 10% — con goal-seek bidireccional, peso económico, y tarjeta Todo Costo / Utilidad $ / % Utilidad que excluye y advierte ítems sin costo); **catálogo NEG** (`catalogo_items`: códigos **INP-NNN** temporales por versión vía `asignarCodigosINP`, incorporación al enviar con prompt por ítem — solo el "sí" quema un **CAT-NNNN** transaccional server-side de contador **acumulativo sin año** — o botón 📚 por ítem; **idempotente** vía `catalogo_id`; buscador dual LPU/catálogo con badges); **agrupador** capítulos/actividades a nivel VERSIÓN (reetiquetado del campo `capitulo` — la entidad "actividad" real quedó para F1.5); **OPEX/CAPEX** opcional (badge + filtro, no sale al PDF); indicador **"hace N días"** escalonado junto a Enviada; **PDF al enviar** (pdf-lib + `@pdf-lib/fontkit`, client-side: formato ISO **CM-FT-CT-19 v05 JUL-2026** modernizado con marca NEG, Montserrat/Lato OFL embebidas desde `public/fonts/`, tabla agrupada según agrupador, totales por esquema, firma con celular del perfil si existe; **SHA-256 en `pdf_hash`** — regla 8 —, un PDF inmutable por versión enviada en `cotizaciones/{id}/v{n}.pdf`, descargable; fallo de generación aborta el envío). Reglas Firestore (`catalogo_items`) y prefijo `CAT` en `generarConsecutivo` desplegados con OK explícito y verificados live. Tests 35 → **46**. Validada contra producción. Pendiente post-F1: campo `celular` en perfiles de usuario (toca `users`, compartido con SST).
- **1.5 ✅ Refinamientos comerciales — COMPLETA (12-jul-2026, puntos 1–4 ✅).** Pendiente de F1.5 solo el backlog PDF (abajo).
  1. ✅ **Evaluador de expresiones matemáticas en campos numéricos** — HECHO (12-jul-2026, **PR #10**, commit de merge **`4190a33`**, validado por Giovanny): `evaluarExpresion()` en `utils/sigp/expresion.ts` acepta `20.23*5`, `1/54`, `(15+3)*2` y evalúa al confirmar (blur/Enter). **Implementado con PARSER PROPIO recursivo-descendente, sin dependencia externa** — mathjs se probó y se DESCARTÓ por peso (+93KB gzip aun restringido por factory); el parser (~70 líneas) cuesta ~1KB y pasa la misma batería de tests (el contrato: 23 tests, incluidos 14 intentos de inyección). **Sin `eval`**: whitelist de caracteres + gramática que solo conoce números, `+ − * /` (menos unario), paréntesis y multiplicación implícita; coma y punto como decimal; sin miles dentro de la expresión; división por cero → error; resultado con precisión completa. Aplicado vía `InputExpresion` en: **cantidades** (todas las filas), **valor unitario** (solo manuales — LPU/catálogo bloqueados por fidelidad), **costo interno del análisis 📊** (con vaciado → limpia costo y margen), y **rendimiento/costo del modal APU** (inválidas bloquean "Aplicar"). Inválida → campo en rojo con motivo, no persiste, conserva el texto.
  2. ✅ **Actividades como entidad propia** — HECHO (12-jul-2026, **PR #8**, commit de merge **`ac287b3`**, validado funcionalmente por Giovanny). Entregado: **modelo** `modo_agrupacion: 'capitulo' | 'actividad'` a nivel versión (default lazy 'capitulo', sin migración; versiones aprobadas intactas) + **`Actividad { id, nombre, orden }`** + **ítem como INSTANCIA** (`instancia_id` uuid local — key de React/asignación; el mismo `codigo` puede repetirse en varias actividades volviéndolo a buscar en el buscador dual); `subtotalesPorGrupo` (antes de impuestos; Σ = costos directos; 'Otros' para huérfanos) integrado a `calcularTotales` (5º parámetro opcional — impuestos byte-idénticos a F1.4); **UI**: selector de modo con siembra capítulo→actividad (una actividad por capítulo; la vuelta es no destructiva), panel de actividades (crear/renombrar libre/reordenar/eliminar → ítems a 'Otros'), select de actividad por ítem; **bloqueo de fidelidad comercial**: ítems de origen LPU o catálogo (`esItemBloqueado`) con código/descripción/unidad/precio en solo lectura y defensa en profundidad en `patchInstancia` (ni por código se muta el snapshot; margen derivado, costo interno editable, cantidad siempre editable) — la duplicación de instancias "Añadir a…" se retiró; **PDF** agrupado por actividades/capítulos con la misma fuente de subtotales. Tests 46 → **63**. Sin cambios en reglas/functions/storage.
  3. ✅ **Numeración de versiones de cara al cliente** — HECHO (12-jul-2026, **PR #9**, commit de merge **`524820a`**, validado por Giovanny): `etiquetaVersion()` en `utils/sigp/formato.ts` (≤1 → sin sufijo; ≥2 → 'vN') aplicada en bandeja, header/banner/botón PDF del detalle, timeline, confirms de acciones, comparación de versiones ("1ª emisión" para v1) y el PDF (título del doc, encabezados). El `version` interno (1, 2, 3…) NO cambia — solo presentación.
  4. ✅ **Máximo 2 decimales en UI y PDF** — HECHO (misma rama, validado): `fmtNum`/`fmtMoney` en `utils/sigp/formato.ts` como fuente única (es-CO, miles '.', decimales ','; enteros limpios) aplicados a cantidades, valores, subtotales por grupo, cuadro de totales, análisis 📊 (costo/margen/peso), modal APU (rendimientos solo-lectura) y todo número del PDF. **Precisión interna intacta**: el recorte es solo de render; los campos editables conservan el valor completo (testeado).
  - 🧰 **Ajuste UX (misma rama)**: ancho de la tabla de ítems del constructor — con Análisis económico ON la vista va a ancho completo (totales/resumen bajan como tarjetas), columnas con min-width justo, descripción con elipsis+tooltip, encabezado y columnas Código/Descripción sticky. **Solo layout** — sin cambio de datos, columnas ni matemática. Verificado sin scroll horizontal a ≥1280px en ambos modos; móvil intacto.

  5. ✅ **Backlog PDF · M1 — rediseño del PDF como "propuesta económica"** — HECHO (15-jul-2026, **PR #11**, commit de merge **`94424b6`**, iterado en 5 rondas con Giovanny hasta versión final). El PDF dejó de ser un "formato de cotización" y se compone como propuesta económica de firma de ingeniería (elegancia/sobriedad/"menos es más"). **El cuadro ISO de control documental superior quedó INTACTO** (premisa dura). Entregado en `utils/sigp/cotizacionPdf.ts`:
     - **Encabezado**: el ASUNTO es el título del documento (14pt, con ajuste editorial: línea 1 respeta el consecutivo, línea 2 a la "Versión N", y si no cabe en 2 líneas baja a 12pt/3 líneas antes de elidir) + subtítulo "Propuesta económica"; consecutivo verde 17pt a la derecha con **"Versión N" discreta debajo** (solo v≥2). El cuadro "Asunto de la oferta" se eliminó.
     - **Tarjeta de datos** (2 columnas, iconografía de línea): cliente, NIT, **CONTACTO** (campo nuevo `Cotizacion.contacto` — prellenado con el contacto principal del cliente al crear, editable en borrador junto al asunto, oculto si vacío), fecha, validez, moneda.
     - **Introducción institucional** (2 líneas): "Apreciado(a) [contacto]:" (o "Apreciados señores:") + saludo genérico sin mencionar actividades específicas.
     - **Tabla**: filas alternadas `#F0F2F0`; grupos sobre fondo blanco con icono+nombre en **gris oscuro** (decisión final: menos verde = más seriedad), subtotal en negro, regla divisoria 1.1pt y aire entre grupos; columna CÓDIGO con respiración interna extra.
     - **Resumen económico**: tarjeta con filas alternadas y barra TOTAL verde (11/13pt — protagonista sin estridencia).
     - **Pie INSTITUCIONAL idéntico en todas las páginas**: logo gris con eslogan y sello BIC (`public/logo-neg-gris.png`, derivado de `LOGOS/Logo Neg con bic vectores-4.png`) 140pt a la izquierda, **QR estático** a `negingenieria.com` al centro (`public/qr-web.png`), redes/web/teléfonos a la derecha. Los datos del cotizador van UNA sola vez, al cierre del contenido (sección "COTIZADO POR" con **área de firma de 48pt** para firma manuscrita/digital futura).
     - **Espacio**: `MARGEN_INF` 152→118; cotizaciones cortas tienden a 1 página sin forzar; títulos de sección nunca quedan huérfanos al pie (reserva del primer contenido en `seccion()`).
     - Del backlog original quedaron resueltos: **a) tagline** — CERRADO 15-jul-2026: Giovanny confirmó *"Ingeniería que cambia el mundo"* (frase inspirada en Isaac Asimov) y el asset `logo-neg-full.png` verificado visualmente YA lo trae correcto (la observación de "CONSTRUYE SOLUCIONES" era del reconocimiento de julio, obsoleta tras el rebrand de mayo); b (grupos huérfanos = 'Otros', por 1.5.2), c (QR/redes reales), e (filas vacías ocultas), f (por 1.5.3/1.5.4), g (iconos vectoriales monocromos, B/N digno); d (multipágina) por M2. **Backlog PDF: sin pendientes.**
  6. ✅ **F1.5.5 — Condiciones elegibles, notas base y cuadro ISO en negro** (15-jul-2026, mismo **PR #11**, merge `94424b6`): **presets de condiciones comerciales** (`PRESETS_FORMA_PAGO/TIEMPO_EJECUCION/GARANTIA` en `types/sigp/cotizacion.ts` — datalist: elegible O texto libre, en creación y en detalle; ajustar las listas con el área comercial); **texto base de "Notas importantes"** (`OBSERVACIONES_BASE` — sembrado al crear, botón "Insertar texto base" en borradores vacíos, una nota por línea → viñetas del PDF); **cuadro ISO todo en letras negras** (sin verde, estructura intacta); banda del encabezado de tabla se probó en gris carbón y **volvió a verde** por decisión de Giovanny. El formato queda a la espera de la revisión del área comercial.
  7. ✅ **Backlog PDF · M2 — robustez multipágina** — HECHO (15-jul-2026, **PR #12**, commit de merge **`c3d0c6d`**, validado por Giovanny con oferta de 36 ítems / 4 páginas). Solo `utils/sigp/cotizacionPdf.ts`: encabezado corrido de páginas 2+ con **control documental "CM-FT-CT-19 · v05"** a la derecha (consecutivo · Versión N a la izquierda; el cuadro ISO completo NO se repite); **keep-with real de grupos** (el encabezado de actividad/capítulo entra solo si cabe con su primera fila completa, medida real); **"(cont.)"** al reanudar un grupo tras salto de página (sin repetir subtotal — evita lectura de doble suma); título de NOTAS reserva la altura de su primera cláusula. Ya existían de M1 y quedaron verificados: filas nunca partidas, encabezado de columnas repetido por página, "Página X de Y" post-paginado, tarjeta TOTAL/condiciones/firma indivisibles, pie institucional idéntico en todas las páginas. Regresión: cotización corta byte-idéntica a M1 salvo la etiqueta del encabezado corrido. Con esto queda cubierto el pendiente d) del backlog (validar además con un acta real de Claro cuando llegue el caso de uso).
  8. ✅ **PDF — descripción completa de ítems** — HECHO (16-jul-2026, **PR #15**, commit de merge **`d2cbb05`**, detectado por Giovanny en el piloto con un ítem de catálogo de alcance largo). La tabla limitaba cada descripción a 2 líneas con elipsis (herencia de M1); ahora se pinta COMPLETA — la descripción es el alcance pactado con el cliente. Tope de 20 líneas solo como seguro anti-desborde (una fila jamás supera la página; el corte por fila completa de M2 se mantiene). Verificado con oferta de 5 páginas (descripciones de 4-5 líneas + grupos "(cont.)").
  9. ✅ **PDF — refinamientos de tabla del piloto** — HECHO (16-jul-2026, **PR #16** merge **`0c90265`** + **PR #17** merge **`e7841ad`**, observaciones de Giovanny sobre COT-2026-010): versales del encabezado centradas ópticamente en la banda de 19pt; DESCRIPCIÓN +22pt de ancho (CANT 46→36, VR. UNITARIO 82→70; montos largos verificados); el primer grupo (y los "(cont.)" tras salto de página) pegado a la banda del encabezado — el aire de 16pt va ENTRE grupos; regla de cierre al final de la tabla.
  10. ✅ **Descarga del PDF con nombre de archivo propio** — HECHO (16-jul-2026, **PR #18**, commit de merge **`497100f`**, pedido por Giovanny): el botón 📄 descarga como blob con nombre "COT-YYYY-NNN [vN] - CLIENTE.pdf" (el atributo `download` no aplica cross-origin sobre Storage); fallback a pestaña si el fetch falla; enlace secundario "ver" para previsualizar. E2E en emulador. **CORS del bucket configurado en producción** (16-jul, vía API de GCS con la service account): sin CORS el fetch del blob fallaba en prod y el botón caía al fallback de pestaña; quedó GET/HEAD para `https://sst-panel-neg.vercel.app` + localhost 5173/5174, `maxAgeSeconds: 3600` — NO toca reglas de seguridad de Storage (la autorización sigue siendo el token de la URL). El emulador permite todo CORS: este tipo de fallo SOLO se ve en producción. ⚠️ Lección del piloto: el PDF se genera EN EL NAVEGADOR — tras cada deploy hay que RECARGAR el panel antes de generar (una pestaña vieja produce el PDF viejo); considerar aviso automático de "versión nueva del panel" como mejora futura.
  11. 🧹 **Limpieza autorizada de producción** (16-jul-2026): las 9 cotizaciones de prueba (COT-2026-001…009) borradas con versiones y archivos de Storage; SOL-2026-002 (Titan) devuelta a `lista_para_cotizar` con historial; el contador de consecutivos NO se tocó (la primera real del piloto es COT-2026-010; los huecos 001–009 fueron pruebas del arranque — dejar nota en el SGI).

**🚀 `sigp_f1_enabled` = `true` — ACTIVADO el 16-jul-2026** (autorizado por Giovanny tras el pre-vuelo de activación: 5 roles × 5 módulos sin errores, reglas prod == HEAD, Functions y Storage sanos). El módulo comercial SIGP es visible en el sidebar para los 6 usuarios con rol SIGP; `tecnico` y `sst` no ven cambios. Reversión: editar el parámetro en Remote Config → `false` → Publicar (2 minutos, sin deploy).

**Rebrand del panel** (16-jul-2026, **PR #13**, merge **`00df9b5`**): el nombre global pasó de "Panel SST" a **"Panel del Sistema de Gestión Integral"** (pestaña, login y sidebar); "SST" se conserva como nombre de área. Solo texto visible.

**UX: LPU vive en el detalle de Cliente** (20-jul-2026, **PR #24**, commit de merge **`0568e01`**, validado por Giovanny — resuelve el pendiente de UX "LPU como pestaña del detalle de cliente"): nueva página `/sigp/clientes/:clienteId` (`ClienteDetalleSigp`) con pestañas **Información** (contactos + condiciones) y **Listas de precios (LPU)** — la tabla de LPUs del cliente (vigente/histórica) y el wizard de importación **pre-scopeado** (selector de cliente bloqueado vía `clienteIdInicial`, capacidad que el wizard ya traía). El ítem "LPU" se retiró del sidebar; **`/sigp/lpus` redirige a Clientes** (enlaces guardados no se rompen); `/sigp/lpus/:lpuId` (detalle de una LPU) sigue vivo con back-link al cliente; `LpusSigp.tsx` eliminado. **Solo navegación/UI**: modelo `lpus`, subcolección items, SheetJS, mapeos, Storage, reglas y el buscador LPU del cotizador quedan INTACTOS (regresión verificada E2E). Cambio VISIBLE en producción al mergear (F1 en vivo; solo Vercel, sin reglas/functions).

**UX: historial de contactos del cliente en Solicitudes** (20-jul-2026, **PR #25**, commit de merge **`09bf349`**, validado por Giovanny — mejora de piloto, F1 en vivo): el formulario de Solicitud reutiliza **`clientes.contactos[]` como historial** — con cliente registrado el contacto se ELIGE de un selector (prefill: el principal, índice 0; tarjeta resumen de solo lectura), y **"＋ Nuevo contacto…"** abre los campos libres y al registrar la solicitud lo AÑADE al cliente vía `arrayUnion` (sin duplicar, comparación por nombre normalizado; el padre recarga clientes → aparece en la siguiente solicitud sin refrescar). Prospecto sigue texto libre; la solicitud guarda el MISMO campo `contacto` de siempre (cotizaciones y preventivos intactos); reglas ya lo permitían (write clientes = puedeGestionarProyectos). Merge visible en producción; sin reglas/functions.

### Estado de F2 (Planeación / Ejecución) — EN CURSO

**TODA la Fase 2 va detrás del flag Remote Config `sigp_f2_enabled` (default `false`)**: con el flag apagado el módulo Proyectos es invisible y el gancho de creación es inerte — F1 en producción queda intacto. Override local vía `.env.local` (`VITE_FF_sigp_f2_enabled=true`), mismo patrón que F1.

- **2.1.a ✅ Ficha de Proyecto (columna vertebral)** — HECHA (18-jul-2026, **PR #19**, commit de merge **`695d027`**, validada por Giovanny en emulador). Entregado: **colección `proyectos`** (`types/sigp/proyecto.ts`: enum completo de 12 estados creado → … → cerrado, labels/colores sin azules; snapshot COPIA de la versión aprobada — cliente/NIT/asunto/contacto/valor_venta/esquema/tipo_inversion/alcance resumido por grupo con `subtotalesPorGrupo`, misma fuente que el PDF); **nacimiento automático al aprobar una cotización** (`utils/sigp/proyectos.ts` + gancho en `CotizacionAcciones`): consecutivo **PRY-YYYY-NNN server-side**, **idempotente por construcción** (doc id = id de la cotización → 1:1 garantizado; re-aprobación/doble clic no duplican; consecutivo preservado ante fallo — patrón SOL/VIS/COT; auto-reparación del enlace inverso `cotizacion.proyecto_id`); chip "🏗 PRY-…" en la cotización aprobada o botón "Crear proyecto" (retry + **mecanismo de backfill manual** de aprobadas previas — previsto, no ejecutado); **bandeja** `/sigp/proyectos` (filtro por 12 estados + buscador) y **ficha** (ciclo de vida visual, "Lo pactado", alcance, línea de tiempo, enlace a la cotización origen, placeholders b/c/d); sidebar gateado por flag + `puedeGestionarProyectosUI`. Tests 92 → **98**. E2E en emulador flag ON (nace PRY-2026-001) y flag OFF (F1 byte-idéntico).
- **2.1.b ✅ Asignación de contratista + permisos de ingreso** — HECHA (18-jul-2026, **PR #20**, commit de merge **`b88d3cc`**, validada por Giovanny en emulador). **Hallazgo Paso 0**: el registro `contratistas` es MÍNIMO (`nombre`, `tipo`, `nit`/`cedula`, `estado: activo|inactivo`) — **la habilitación ES el campo `estado`** (administrada por Gestión Administrativa vía la regla de F0.6.a-ter); NO hay campos de evaluación/reevaluación/puntaje: la evaluación formal vive en el SGI (FT Selección y Reevaluación de Proveedores). Entregado: **`proyecto.asignacion`** (snapshot de nombre + documento + `habilitacion_snapshot` con fuente y fecha de consulta — evidencia ISO 9001 §8.4 / 45001 §8.1.4 de proveedor calificado al asignar; `evaluacion_snapshot` OPCIONAL previsto para cuando el registro evolucione; `nota_criterio`), **gate no-solo-UI** (`construirAsignacion` LANZA si el contratista no está habilitado; el selector solo lista habilitados con conteo "N de M"), y **`proyecto.permisos`** (solicitado/aprobado/negado/no_requiere + fechas, entidad responsable, nota y adjunto a Storage `proyectos/{id}/permisos/` — patrón visitas). Transiciones: creado → contratista_asignado → permisos_en_tramite (el primer registro de permisos transiciona; la resolución se registra sobre el mismo objeto), todas con historial. Componentes `AsignacionContratista` y `PermisosIngreso` en la ficha (los placeholders quedan solo para c/d). Tests 98 → **103**. E2E completo sobre PRY-2026-001 (asignación con evidencia congelada verificada en Firestore + adjunto verificado en Storage). Cero archivos de F1 tocados.
- **2.1.c ✅ Preliquidación + aprobación + anticipo + preliquidación del contratista** — HECHA (19-jul-2026, **PR #21**, commit de merge **`091c0a2`**, validada por Giovanny con sus dos ajustes). Entregado: **`proyecto.preliquidacion`** — `valor_venta` AUTO del snapshot mostrado con el **desglose del esquema aprobado** (AIU: obra + A/I/U + IVA sobre la Utilidad; IVA pleno: subtotal); palanca de **MARGEN % con la convención EXACTA del APU** del cotizador (margen = % de utilidad sobre el total → `contratista = venta × (1 − m/100)`, helper `contratistaDesdeMargen`), **bidireccional** (teclear valor_contratista con expresiones deriva el margen — goal-seek como el APU; test cruzado contra `precioDesdeCosto`/`margenDesdePrecio`); `anticipo_pct` default 50 con derivados `anticipoValorDe`/`saldoValorDe`; **vista interna del alcance aprobado CON valores** (misma agrupación del PDF, plegable) + **`observaciones` por ítem** (keyed por `claveItemAlcance`: instancia_id → fallback código+índice/índice; editables en la vista interna, persisten al blur, SÍ salen al contratista). **SEGREGACIÓN DE FUNCIONES**: proyectos DEFINE (`preliquidacion_definida`, estado nuevo — el ciclo quedó de 13); **gerencia_administrativa APRUEBA y registra el ANTICIPO girado** (fecha/valor/evidencia a Storage `proyectos/{id}/anticipo/`) → `preliquidacion_aprobada` → `anticipo_girado` (permisos UI `puedeAprobarPreliquidacionUI` + `veProyectosUI` para que gerencia vea el módulo; E2E con dos usuarios distintos: `definida_por ≠ aprobada_por`). **PDF de preliquidación del contratista** (`utils/sigp/preliquidacionPdf.ts`, marca NEG): alcance por grupos con columnas CÓDIGO/DESCRIPCIÓN/CANT/UND/**OBSERVACIONES** (texto que envuelve) y SOLO anticipo + saldo + TOTAL CONTRATISTA — **jamás valor de venta, utilidad ni precios por actividad** (es lo que hoy se manda por WhatsApp). Matiz de reglas: la regla nueva restringe a gerencia a sus campos; el bloqueo de que proyectos apruebe es de UI (hardening por-campo anotado como opcional). Tests 103 → **108**. El pago del cliente y el saldo del contratista son F2.1.d.
- **2.1.d ✅ Ejecución + entrega + soporte del cliente + evaluación — F2.1 COMPLETA** — HECHA (20-jul-2026, **PR #22**, commit de merge **`1d2118e`**, validada E2E por Giovanny). **Máquina de estados final (15)**: `informe_entregado` reemplazado por la secuencia real `en_ejecucion → ejecutado → entregado_cliente → soporte_recibido → enviado_a_facturacion`; los estados desde **`facturado`** (`ESTADO_INICIO_ADMINISTRATIVA`) son el **MÓDULO FUTURO de Gerencia Administrativa** (factura → pago del cliente → saldo al contratista con su gate de pago-cliente-primero) — marcados en la UI (chips punteados + nota) y FUERA del alcance de Proyectos. Entregado: **ejecución MVP** (`proyecto.ejecucion`: iniciar; "ejecutado" con **evidencia fotográfica OBLIGATORIA** multiarchivo a Storage `proyectos/{id}/ejecucion/` + nota; el avance por actividad y el informe fotográfico automático quedan para F2.3); **entrega** (`proyecto.entrega`: fecha + nota); **soporte del cliente** (`proyecto.soporte_cliente`: tipo `orden_pago|orden_compra|liquidacion` + número + fecha + adjunto a `proyectos/{id}/soporte/` + **verificación "concuerda con lo ejecutado" que BLOQUEA el avance**); **handoff** con confirm y banner "En manos de Gerencia Administrativa"; **evaluación del contratista** (`proyecto.evaluacion_contratista`: 4 criterios ISO — calidad/cumplimiento/SST/documentación — puntaje 1–5, promedio derivado guardado, comentario; `CRITERIOS_EVALUACION` extensible por GI; disponible desde "ejecutado"). Todo con historial. Componentes `EjecucionProyecto` y `EvaluacionContratistaCard`. Tests 108 → **111**. E2E completo: anticipo_girado → … → enviado_a_facturacion con 2 fotos + OC adjunta en Storage + evaluación promedio 4,5.

- **2.2 ✅ Entrada de preventivos IHS — F2 COMPLETO** — HECHA (20-jul-2026, **PR #23**, commit de merge **`a89519c`**, validada por Giovanny incl. el ajuste de cliente fijo). Entregado: **matriz de precios versionada EN CÓDIGO** (`types/sigp/preventivos.ts`: 9 renglones zona Z1/Z2/Z3 × greenfield pesado/liviano + rooftop pesado, valores normal/jungle, transporte constante $1.080.000; mapa departamento→zona insensible a tildes; `precioPreventivo()` puro: base jungle/normal + transporte si jungle O San Andrés; rooftop liviano → "no disponible"; esquema **IVA pleno** — el IVA se aplica aguas abajo en la facturación de Administrativa; **no requiere colección ni regla Firestore**, cambia por PR); **solicitud tipo `preventivo`** (`tipo: comercial|preventivo`, ausente = comercial → flujo de F1 INTACTO; datos del sitio con zona/SAI denormalizados; **cliente FIJO IHS** — resuelto del registro por nombre, solo lectura con NIT y candado; sin IHS activo → aviso y bloqueo; badge PREV en bandeja); **aceptar/rechazar SIN cotización** (panel dedicado con precio de matriz en vivo; rechazar exige motivo → descartada; **aceptar crea el proyecto `origen='preventivo'`** — idempotente con doc id = id de la solicitud, PRY preservado ante fallo, snapshot con valor_venta de matriz y alcance de un renglón; estado nuevo `aceptada` terminal inalcanzable desde la máquina comercial); el proyecto **entra al flujo F2.1 completo** (ficha con origen "preventivo SOL-…", preliquidación con palanca de margen sobre el precio de matriz, doc del contratista desde el snapshot). Tests 111 → **120** (9 de matriz: normal, jungle, SAI, jungle+SAI transporte único, combinaciones inexistentes). E2E: preventivo Antioquia/Z3/GF-pesado/jungle → $2.856.685 → aceptado → PRY con F2.1 recorrido hasta preliquidación (margen 30% exacto).

- **2.3 ✅ Entregables IHS (versión LIGERA)** — HECHA (21-jul-2026, **PR #26**, commit de merge **`d497e6e`**, validada por Giovanny). **Decisión de proporcionalidad**: se DESCARTÓ el enfoque pesado de F2.3.a (captura de datos + generación del Excel con ExcelJS sobre la plantilla oficial) — los 3 formatos IHS los diligencia el equipo en los ARCHIVOS DEL CLIENTE y se suben a la app de IHS; replicarlos en el panel era doble digitación. El panel solo TRAZA y guarda copia. Entregado: **`proyecto.entregables_ihs`** (solo `origen='preventivo'`) — checklist de los 3 formatos (**Inventario de antenas · Estado de línea de vida · Torque de torre**), cada uno con estado pendiente/diligenciado, **adjunto OBLIGATORIO** (la copia del Excel → Storage `proyectos/{id}/entregables/`, regla ya desplegada), fecha, nota y entrada al historial; **indicador X/3** y **GATE en la entrega**: con faltantes, el paso "Entrega al cliente" lista cuáles faltan y oculta el botón — no se llega a `enviado_a_facturacion` sin los 3 (helpers puros `entregablesIhsFaltantes`/`Completos`; para no-preventivos no aplica). Componente `EntregablesIhs` + aviso en `EjecucionProyecto`. Tests 120 → **123**. E2E: 0/3 bloquea con lista → 3/3 libera; proyecto de cotización sin sección ni gate.

- **Panel SIGP · Tablero de indicadores (ISO 9.1) ✅** — HECHO (21-jul-2026, **PR #27**, commit de merge **`47fafc3`**, validado por Giovanny; visual v2 del mockup `panel_sigp_mockup_v2.html`, alineado al Dashboard SST). Reemplaza el placeholder de `/sigp/panel`. **CAPA OFICIAL**: los 5 indicadores de la Caracterización Integral (`utils/sigp/indicadores.ts`, cálculo puro client-side + semáforos por meta): 1) cumplimiento del plan (actividades ejecutadas/programadas, corte actual, meta 80–100 %), 2) calidad (entregas con calificación ≥4/5 del periodo, ≥90 %), 3) proyección presupuestal (Σ costo ejecutado/Σ valor cotización, 90–110 % con ámbar simétrico), 4) satisfacción (encuestas ≥4/5 del periodo, ≥90 %), 5) ambiental/SST (**registro MANUAL** por periodo en la colección **`indicadores`** doc `sst_YYYY-MM` hasta integrar el Panel SST — regla **DESPLEGADA en producción el 21-jul** con OK, pre-verificación prod==HEAD, post-verificación byte-idéntica y prueba de denegación anónima). **CAMPOS NUEVOS de captura**: `actividades_plan[]` (checkboxes en Ejecución, sembrado del alcance al iniciar), `entrega.calificacion_calidad` (1–5 OBLIGATORIA en el acta), `preliquidacion.costo_ejecutado` (con expresiones), `evaluacion_cliente` (encuesta 1–5, tarjeta `SatisfaccionClienteCard`). **VISUAL v2**: hero "Así vamos" (gradiente verde→violeta, frase-resumen dinámica, anillo de salud X/5, selector de periodo, chip ISO 9.1); tarjetas KPI con chip de ACENTO por identidad (paleta categórica CVD: verde #628E3A · violeta #6E56CF · ámbar #E0A100 · teal #1BAF7A · naranja #EB6834), anillo conic en color de ESTADO (#3C8B2E/#E0A100/#D03B3B) SIEMPRE con pill de texto, sparkline 6 periodos (solo ind. 2/4 — los de corte degradan sin inventar series); operativa: dona categórica de estados agrupados, funnel verde con conversión, tiles IHS (con `ejecutados`). Regla de color: identidad ≠ estado, nunca mezclados. Tests 123 → **132**. NOTA de marca: la paleta categórica del tablero fue validada explícitamente por Giovanny para dashboards (excepción consciente a la regla "solo verde"; el azul sigue prohibido).

**PDF de cotización — tabla compacta y rebalanceada** (22-jul-2026, **PR #28**, commit de merge **`5bddb6f`**, iterado en 3 rondas con Giovanny — cambio VISIBLE en producción, F1 en vivo; solo `utils/sigp/cotizacionPdf.ts`): cuerpo de tabla 8→7.4pt / interlineado 11→9.2 / padding de fila 10→6; encabezados de grupo 37→26pt; **columnas rebalanceadas** (CÓDIGO 54→40, UND 40→34, VR. UNITARIO 70→58, VR. TOTAL 92→74 → DESCRIPCIÓN +24 %); títulos de la banda verde TODOS centrados sobre su columna (y códigos de fila centrados); **centrado VERTICAL de todas las celdas** respecto del centro de la fila (bloques multilínea como bloque); tope anti-desborde de descripción 20→40 líneas (la métrica compacta recortaba alcances largos — fidelidad); **piso especial de la firma** (100pt — puede acercarse al pie en vez de abrir hoja casi vacía); reserva del pie 118→108; condiciones comerciales SIGUEN como sección propia (se probó lado-a-lado del resumen y Giovanny lo descartó por amontonado); aire tras el cuadro informativo (+26pt) e introducción priorizando respiro sobre densidad (decisión explícita de Giovanny: el caso típico de 3 renglones puede ir a 2 páginas). Resultado con COT-2026-013 real: 2 páginas con la firma acompañada en la 2ª. Validado con 6 casos renderizados página a página + E2E en emulador (envío desde el panel, `pdf_hash` coincidente). Intactos: cuadro ISO, fidelidad LPU, iconografía, marca, QR, pie.

**Asunto canónico enlazado solicitud ↔ cotización — Bloque B** (22-jul-2026, **PR #29**, commit de merge **`2d02c9c`**, validado E2E — cambio VISIBLE en producción, F1 en vivo; sin cambios en reglas): el asunto vive en la **solicitud** (`solicitudes.asunto`, nuevo, opcional, retrocompatible) y las cotizaciones enlazadas llevan un ESPEJO escrito SOLO vía **`utils/sigp/asunto.ts`** (batch atómico solicitud + todas sus cotizaciones — editable desde cualquiera de los dos lados sin divergir; planificador puro testeable `planPropagacionAsunto`). Solicitud: campo opcional al registrar + edición inline en el detalle. Cotización: precarga al crear desde solicitud (y escribe de vuelta), indicador 🔗, propagación al guardar borrador. Proyecto: `snapshot.asunto` ya se congelaba al nacer — editar después NO lo toca (verificado). **PDF**: con PDF enviado vigente, el cambio de asunto marca `pdf_desactualizado` (badge ámbar) — el PDF enviado NO se regenera (evidencia inmutable con hash, regla 8; el reenvío pasa por Nueva versión); la marca se limpia al enviar / nueva versión / aprobar. Preventivos no usan asunto (título de matriz). Tests 132 → **137**.

**Transporte automático por zona en correctivos IHS — Bloque C** (22-jul-2026, **PR #30**, commit de merge **`a69294f`**, validado E2E — cambio VISIBLE en producción, F1 en vivo; sin cambios en reglas/functions/PDF): en el constructor de cotización, la línea de transporte deja de teclearse. **Hallazgo Paso 0**: el LPU real de IHS NO tiene grupo "TRANSPORTE" — las 8 filas de zona viven en ESTRUCTURAS (51-54) y OBRA CIVIL (163-166); reconocimiento por el DATO (unidad `%` + descripción "Zona N…", verificado contra las 536 filas del LPU vigente) + flag `es_transporte` al insertar. **Selector "🚚 Transporte por zona"** (solo borradores, aparece si el LPU vigente trae filas de zona — IHS de facto, nada cableado por cliente): elegir zona = única línea de transporte (cambiar reemplaza; el buscador rechaza una segunda con toast); **sugerencia por el sitio de la solicitud** leyendo los departamentos de las descripciones del propio LPU (frontera de palabra). **Única lógica nueva** (`aplicarTransporte` en `types/sigp/cotizacion.ts`, pura): cantidad = Σ(demás)/100 a 2 decimales EXCLUYÉNDOSE a sí misma, recalculada en vivo y siempre al persistir; cantidad bloqueada en la fila (⚙) y en `patchInstancia`; factor solo lectura (fidelidad LPU). Tests 137 → **151** (incl. réplica exacta de la COT-2026-013: cantidad 28.051,15 → $442.647; cierre E2E $592.647 + IVA $112.603 = $705.250).

**Obra-espejo SST desde el proyecto — Bloque D** (22-jul-2026, **PR #31**, commit de merge **`d3a718e`**, validado E2E con UI real — cambio VISIBLE en producción al mergear; SIN cambios en reglas Firestore ni en la app Flutter): el proyecto SIGP alimenta el panel SST creando una obra en la colección `obras` (la que la app YA lee) al entrar en ejecución. **`utils/sigp/obraEspejo.ts`**: upsert IDEMPOTENTE con id **`pry_{proyectoId}`** (1:1 por construcción, reintentar no duplica); contrato EXACTO de la app verificado campo por campo contra `obra_model.dart` y docs reales (`nombre_sitio` = sitio + alcance corto + mes/año · `codigo` = PRY-YYYY-NNN · `cliente` · `nombre_completo` "sitio | codigo | cliente" · `estado` · `fecha_creacion`; extras solo-SIGP `proyecto_id`/`proyecto_consecutivo`/`origen:'sigp'`). **Ciclo** (un solo escritor: el proyecto): `activa` desde `en_ejecucion` → `inactiva` desde `enviado_a_facturacion` (decisión 22-jul); disparo en `transicion()` de EjecucionProyecto (fallo del sync NO revierte el proyecto); botón "🏗 Sincronizar obra SST" en la ficha (reintento/backfill, gateado desde `en_ejecucion` + guarda anti-obra-prematura en el helper). **Panel Obras → "Obras activas"** (vista informativa): espejos con chip SIGP e identidad/estado bloqueados; creación manual degradada a fallback secundario (gestores); roles SST (`sst`/`residente_sst`/`gestion_integral`) ahora VEN la vista en solo lectura (técnicos se asignan desde Usuarios por ID de doc, mecanismo intacto). **Prerequisito ejecutado el mismo día**: limpieza AUTORIZADA de `obras` en producción — respaldo completo en `_respaldos/obras-backup-2026-07-22.json`, hard delete de 15 obras VACÍAS (0 formularios, re-verificado en vivo), conservadas las 5 CON evidencia (ALKARAWI 56 forms · PISO SHUT 15 · SAN BERNARDO 4 · Megacenter 4 · CHOCONTA 3), 0 referencias colgantes. **Go-live/backfill**: proyectos ya en ejecución antes del deploy → un clic en "Sincronizar obra SST" desde su ficha; smoke test real pendiente de Giovanny (ejecución → obra en app → técnico → facturación → inactiva). Tests 151 → **158**.

**Nombre y código del sitio de punta a punta — Bloque 1 de mejoras** (22-jul-2026, **PR #32**, commit de merge **`a7166d6`**, validado E2E con UI real en ambos flujos — visible en producción; sin reglas/functions/app): la identificación del sitio se captura LIMPIA en el origen y viaja hasta la obra SST. **Solicitud**: `nombre_sitio` (requerido) + `codigo_sitio_cliente` (requerido, **'N/A' válido**); en preventivos IHS se autocompletan del sitio capturado (editables). **Cotización**: hereda ambos (editables en borrador). **Proyecto**: congelados en el snapshot al nacer (ambas vías); bandeja con columna Sitio (+búsqueda) y ficha en "Lo pactado". **Obra-espejo**: `nombre_sitio` limpio + `codigo` del cliente → `nombre_completo` "SITIO | CÓDIGO | CLIENTE"; **el PRY sale de `codigo`** (queda en `proyecto_consecutivo`, chip en la tabla de Obras). Fallback legacy: snapshots sin sitio (proyectos pre-Bloque 1) siguen con título derivado + código PRY. Docs históricos sin los campos: válidos (opcionales en el tipo; requeridos solo al registrar nuevas). Tests 158 → **159**.

**"Obras" al grupo SST del sidebar — Bloque 6 de mejoras** (22-jul-2026, **PR #33**, commit de merge **`c724d49`**, trivial validado en navegador): el ítem de navegación "Obras" vive SIEMPRE en el grupo SST (la vista es informativa SST desde el Bloque D; en SIGP basta "Proyectos"). Se retiró el ítem `/sigp/obras` del grupo SIGP y la regla que ocultaba `/obras` cuando el usuario tenía acceso SIGP. Sin cambios de permisos ni de la vista; rutas `/obras` y `/sigp/obras` siguen vivas. Solo `Sidebar.tsx`.

**Tipo de inversión solo para clientes con el flag — Bloque 2 de mejoras** (22-jul-2026, **PR #34**, commit de merge **`5ee967b`**, validado E2E): flag **`usa_tipo_inversion`** en el cliente (bool, default false, ausente = false; checkbox en el CRUD dentro de Condiciones comerciales). El selector OPEX/CAPEX solo aparece en "Nueva cotización" si el cliente lo tiene (sin flag: ni se muestra ni se guarda); mismo gating en la barra Inversión del detalle, con excepción deliberada para cotizaciones que YA traen `tipo_inversion` (dato histórico visible/limpiable). **Pendiente post-merge (clic de Giovanny)**: marcar el checkbox en el cliente Claro real de producción.

**Modelo contratista ↔ técnicos — Bloque 3+5 de mejoras** (22-jul-2026, **PR #35**, commit de merge **`66897f5`**, validado E2E con el trigger cargado en el emulador): (#5) SST fija/corrige el **empleador del técnico** (`contratista_id`/`nombre`, que la app ya captura al registrarse) desde el modal "Asignar obras" + **filtro por contratista** en la tabla de Técnicos; **vínculo principal ↔ técnico** (`contratistas.usuario_tecnico_id`, mismo individuo, editable en el CRUD — la app lo ignora); la **obra-espejo lleva `contratista_id`** (de la asignación del proyecto); (#3a) **Cloud Function `asignarObraAlPrincipal`** (`functions/obraEspejo.js`, 2ª gen, trigger `obras/{obraId}`, Admin SDK): obra sigp activa → contratista → técnico vinculado → arrayUnion en `obras_asignadas`; idempotente; sin vínculo se omite sin error. **Los gestores NO escriben `users`** — en la primera iteración se probó una regla `hasOnly(['obras_asignadas'])` para gestores y **se descartó por decisión de Giovanny** (reglas byte-idénticas a main; un solo camino de escritura: la CF con Admin SDK). (#3b) **Aval SST**: con empleador fijado, el modal lista SOLO las obras del contratista del técnico (más las ya asignadas); sin empleador, sin filtro (retrocompat). Tests 159 → **160**. **CF DESPLEGADA en producción el 22-jul con OK explícito** (pre-deploy verificado: functions prod == HEAD; primer intento falló por propagación de Eventarc — esperado en el primer trigger del proyecto — y el reintento salió limpio). **Backfill de cierre ejecutado por script (autorizado)**: vínculo `contratista_004` → usuario técnico David + `contratista_id` en la obra espejo existente + touch → el trigger asignó la obra a David EN PRODUCCIÓN (verificado en su doc). Sin deploy de reglas.

**Corrección de preliquidación con trazabilidad ISO 7.5 — Bloque 4, CIERRA LA TANDA de 6 mejoras del piloto** (22-jul-2026, **PR #36**, commit de merge **`8b4fcbe`**, validado E2E con 3 roles): botón **"✎ Corregir"** (gestores) en `definida`/`aprobada`/`anticipo_girado` con palanca margen↔valor, resumen del cambio en vivo y **motivo OBLIGATORIO**. Antes de aprobar: guarda directo con traza en `historial[]` (quién, cuándo, campo, antes → después, motivo). Después de aprobar: confirm explícito → **REVIERTE a `preliquidacion_definida`**, retira la aprobación del dato vivo (queda en historial) y **exige re-aprobación** — nunca un cambio silencioso. **Anticipo ya girado**: helper **`saldoRealDe`** (saldo contra el valor GIRADO, hecho consumado — no el % teórico) usado en ficha y PDF del contratista; advertencia con saldo recalculado; **SOBRE-GIRO explícito** (saldo negativo nunca recortado: alerta roja "pagado de más por $X" en form y ficha, verificación pedida por Giovanny); la **re-aprobación con giro previo vuelve DIRECTO a `anticipo_girado`** (no pide re-girar). Segregación intacta; sin cambios de reglas ni functions. Tests 160 → **167**. — **TANDA COMPLETA: Bloques 1 (PR #32), 6 (#33), 2 (#34), 3+5 (#35 + CF desplegada), 4 (#36).**

**MÓDULO GERENCIA ADMINISTRATIVA · Bloque 1 — base + registro de factura** (22-jul-2026, **PR #37**, commit de merge **`4c461ed`**, validado E2E con 3 roles; **regla DESPLEGADA en producción el 22-jul 19:45 UTC con OK explícito**): arranca la continuación del handoff. Principio rector: **el SIGP REGISTRA y controla — NO ejecuta dinero ni genera factura electrónica** (sistemas externos del área). Transición `enviado_a_facturacion → facturado` activada (la dispara el registro). **Vista "Facturación y Pagos"** (`/administrativa/facturacion`): bandeja desde el handoff en adelante, búsqueda, por-facturar primero, defensiva ante docs malformados; **grupo nuevo ADMINISTRATIVA en el sidebar** (después de SIGP — continuación del ciclo, fuera de la gestión de proyectos; flags de F2 + rol). **Registro**: `proyecto.facturacion { numero, fecha, valor, cufe?, adjunto PDF? (Storage proyectos/{id}/factura/), registrado_por, fecha_registro }` con valor prellenado del pactado + advertencia si difiere, historial completo y tarjeta de lectura en la ficha. Roles: `gerencia_administrativa` OPERA; `admin`/`gerencia_general` LECTURA. **Regla**: `hasOnly` de gerencia + `'facturacion'` (sigue sin tocar snapshot/asignación/alcance) — pre y post-verificada byte-idéntica. ⚠ **GOTCHA de deploys de reglas**: comparar prod vs repo NORMALIZANDO CRLF/LF (el working tree de Windows produce falsos-negativos). **Ítem futuro opcional (anotado, no bloquea)**: hardening por-campo de los GESTORES en `proyectos` (hoy tienen write total a nivel reglas; que no facturen es de UI y confianza interna — la dirección crítica, gerencia acotada, SÍ está en reglas). El diseño completo del módulo vive en el Project de Claude.ai (`PROPUESTA_MODULO_GERENCIA_ADMINISTRATIVA.md` — NO está en el repo; Giovanny inlineará el spec por bloque). Tests 167 → **169**. Siguiente: Bloque 2 (pago del cliente).

**Obras restringida + creación manual bloqueada — Hotfix A** (23-jul-2026, **PR #38**, commit de merge **`ce17680`**; **regla DESPLEGADA en producción el 22-jul 20:41 UTC** con pre/post-verificación byte-idéntica normalizando CRLF/LF): la vista Obras (sidebar + `/obras` + legacy `/sigp/obras`) queda SOLO para `admin, sst, residente_sst, gestion_integral, gerencia_administrativa, gerencia_general` (alta dirección en lectura — decisión de Giovanny); los roles de proyectos trabajan en Proyectos. Botón "Obra manual (fallback)" solo admin (escotilla). **Regla de `obras`**: `create` solo admin O gestor con `origen == 'sigp'` (la obra-espejo del flujo nace; la manual suelta bloqueada); lectura y update/delete de gestores intactos (la sincronización los necesita). **BUG corregido de paso**: `redirectTo="/obras"` como aterrizaje universal generaba BUCLE infinito al restringir la vista → fallback ahora `/sigp/panel`; cadenas verificadas E2E sin loops para sst pura (aterriza en su Hub de Registros), director, admin y gerencia. Validación de reglas en caliente: crear manual como gestor → 403; espejo `origen:'sigp'` → 200.

**Corrección de preliquidación visible — Hotfix B** (23-jul-2026, **PR #39**, commit de merge **`5ce5f1d`**, solo UI): el botón de corrección del Bloque 4 vivía pequeño al fondo de la sección — ahora es **"✎ Corregir preliquidación"** en el ENCABEZADO de la sección (junto al chip de estado, ámbar evidente, tooltip de reversión); el duplicado del fondo se eliminó. Sin cambios de permisos/estados. Verificado E2E con `operacion_comercial` en el caso real (anticipo girado + costo digitado → corrige con motivo, revierte con traza; costo y giro intactos). **Ruta para guiar usuarios: Proyectos → ficha → sección Preliquidación → botón ámbar arriba a la derecha.**

**Corrección de preliquidación en el tramo de ejecución — Hotfix B revisado** (23-jul-2026, **PR #40**, commit de merge **`09cc93c`**): la causa del reporte "el botón no aparece" era que el proyecto real estaba en `ejecutado` (el chip verde de la sección muestra el GIRO, no el estado) y el gate del Bloque 4 llegaba solo hasta `anticipo_girado` — corto para el caso real (el error se descubre al digitar el costo real, al cierre de la ejecución). **Gate ampliado**: corregible de `preliquidacion_definida` a `enviado_a_facturacion` inclusive (desde `facturado` = territorio administrativo). Pre-ejecución: Bloque 4 intacto (reversión + re-aprobación). **Tramo de ejecución: AJUSTE de pura trazabilidad** — valores + motivo + historial, marca **`preliquidacion.ajuste_pendiente_liquidacion`** (chip ámbar "Ajuste pendiente de reconocer en liquidación"), CONSERVA la aprobación original (válida para el anticipo), sin re-aprobación y **sin frenar jamás el avance**; la reconciliación es de Gerencia Administrativa en la **LIQUIDACIÓN (Bloque 3 del módulo administrativo)** viendo preliquidación + ajustes + costo real. E2E con el escenario exacto de la captura de prod (ajuste "compra olvidada" + avance a `entregado_cliente` con flag persistente). Tests 169 → **173**.

**Pago del cliente — Módulo Gerencia Administrativa · Bloque 2** (23-jul-2026, **PR #41**, validado E2E con 3 roles; **regla `pago_cliente` PENDIENTE de deploy** — sale junto con la del 3a): transición `facturado → pagado_cliente` activada por el registro del pago. **`proyecto.pago_cliente { fecha, valor, medio (transferencia|consignacion|cheque|otro), comprobante? (Storage proyectos/{id}/pago/), registrado_por, fecha_registro }`** — UN pago total (moldeable a abonos futuros), valor prellenado del facturado + advertencia si difiere, historial completo. Bandeja con columnas Factura/Pago + contadores por-facturar/por-cobrar; tarjeta de lectura en la ficha. Regla: `pago_cliente` añadido al hasOnly de gerencia. Tests 167 → **173** (con los 4 del Hotfix B revisado en la misma tanda).

**Gate SST de verificación de contratistas — Módulo Gerencia Administrativa · Bloque 3a** (23-jul-2026, **PR #42**, commit `12f4f5b`, validado E2E; **reglas y CF PENDIENTES de deploy**): antes de liquidar, SST confirma que el contratista está **al día en formatos** (criterio MANUAL de SST — nada cableado). **CONFIDENCIALIDAD (ajuste revisado por Giovanny): SST NO tiene acceso a `proyectos`** (valor de venta, márgenes, preliquidación) — su cola vive en la **proyección delgada `verificaciones_sst/{proyectoId}`** (SOLO identidad + estado: proyecto_id, consecutivo, nombre_sitio, codigo_sitio_cliente, cliente_nombre, contratista_id/nombre, estado, obra_id — tipo `VerificacionSst` con guardarraíl "PROHIBIDO agregar campos económicos"), mantenida por la **Cloud Function `sincronizarVerificacionSst`** (`functions/verificacionesSst.js`, trigger sobre `proyectos/{id}`, un solo escritor server-side; el merge JAMÁS toca `sst_gate`, que lo posee SST). Vista **"Verificación de contratistas"** (`/verificacion-contratistas`, grupo SST del sidebar): cola facturado/pagado_cliente, acciones ✔ Al día / ⚠ Con novedad (observación OBLIGATORIA), traza del gate (de→a) en el historial de la proyección, enlace a los formularios de la obra-espejo (la evidencia sobre la que SST decide); roles `sst`/`residente_sst` operan, `admin` solo lectura. Chip del gate (solo lectura, con observación en tooltip) en la bandeja de Facturación y Pagos. **Reglas**: update de la proyección solo sst/residente_sst por `hasOnly(['sst_gate','historial','fecha_actualizacion'])`; **`liquidado_contratista` exige `get(verificaciones_sst/$(id)).data.sst_gate.estado == 'al_dia'`** en gestores Y gerencia (gate NO esquivable — condición lista para el 3b); gestores NO leen la proyección (si el 3b quiere mostrar el gate en la ficha, se agrega ahí). E2E: cero datos económicos a SST (grep de página + inspección campo por campo), 14/14 casos de reglas con tokens reales, CF re-sincroniza estado en vivo. Tests 173 → **179**.

**Liquidación del contratista — Módulo Gerencia Administrativa · Bloque 3b, CIERRA EL CORAZÓN DEL CICLO** (23-jul-2026, **PR #43**, commit de merge **`1e23c51`**, validado E2E; regla desplegada tras el merge): factura → pago → verificación SST → **liquidación**. **MODELO NEG**: la preliquidación es MANO DE OBRA (NEG compra los materiales); lo que el contratista compra y NEG le reconoce va en **LÍNEA PROPIA `compras_reembolsos`** (capturada por los GESTORES durante el proyecto en la ficha — concepto, valor con expresiones, soporte a Storage `proyectos/{id}/compras/`, traza; congelada tras liquidar). **Acción "Liquidar contratista"** (solo `gerencia_administrativa`, bandeja Facturación y Pagos) para `pagado_cliente` CON gate SST `al_dia` (sin aval: botón deshabilitado + regla 403 vía `get(verificaciones_sst)` — ya estaba viva): concilia mano de obra + compras = total final; ajustes de ejecución RECONCILIADOS (flag `ajuste_pendiente_liquidacion` retirado, motivos snapshot en `ajustes_reconocidos`); **retenciones MOLDEABLES** (concepto libre + valor, lista vacía por defecto — los tipos se definen con la reunión del área, nada cableado); **saldo final = total − anticipo GIRADO real − retenciones** (sobre-giro visible, jamás recortado) → `liquidado_contratista` con traza completa; tarjeta de lectura en la ficha. **PDF de Liquidación** (`utils/sigp/liquidacionPdf.ts`, análogo al de preliquidación; jamás pinta venta/utilidad/margen): sello de conciliación con **ATRIBUCIÓN HONESTA del origen** — helper puro `origenDiferenciaLiquidacion` → `igual | compras | ajustes | compras_y_ajustes`, aplicado en PDF + modal + ficha (catch de revisión: el caso trampa "ajuste sin compras" habría impreso "+$0 por compras/reembolsos" en un documento que se entrega al contratista); alcance por grupo, conciliación económica, barra SALDO A PAGAR. **Regla**: `+ 'liquidacion','compras_reembolsos'` al hasOnly de gerencia. E2E: caso igual / con diferencia / mixto, gate bloqueante (UI + regla), segregación (gestores capturan compras pero NO liquidan; SST sin nada económico). Tests 179 → **190**. Pendiente no bloqueante: revisión visual del PDF por Giovanny. Siguiente: cierre (`liquidado_contratista → cerrado`), el último y más simple del ciclo.

**Cierre del proyecto + bandeja "Gestión Administrativa" — Bloque final del ciclo** (23-jul-2026, **PR #44**, commit de merge **`75e8d6c`**, validado E2E; regla desplegada tras el merge): el flujo queda de punta a punta (solicitud → … → liquidación → CIERRE) y gerencia tiene su centro de mando. **CIERRE**: acción "🏁 Cerrar proyecto" (gerencia_administrativa + admin) solo en `liquidado_contratista`; modal con **resumen de COMPLETITUD informativo** (✓/pendiente de factura, pago, liquidación, evaluación del contratista y satisfacción del cliente — la satisfacción pendiente ofrece enlace a la ficha SIN bloquear) + notas/lecciones aprendidas → `proyecto.cierre {fecha, cerrado_por, notas}` + estado `cerrado` + traza. **Cerrado = SOLO LECTURA** (banner en ficha, `puedeGestionar=false` en un solo punto) e **INMUTABLE a nivel de reglas** (update Y delete denegados para gestores y gerencia; a `cerrado` solo desde `liquidado_contratista` en ambas rutas de update). **BANDEJA** (renombra "Facturación y Pagos", misma ruta `/administrativa/facturacion`; ítem del sidebar → "Gestión Administrativa"): **7 secciones con contador vivo** — Por aprobar · Por girar anticipo · Por facturar · Por cobrar · Por liquidar · Por cerrar · Cerrados/Histórico — + "Todo el ciclo"; `SECCIONES_ADMINISTRATIVA`/`enBandejaAdministrativa` en `proyecto.ts`; los estados de EJECUCIÓN quedan fuera (territorio de proyectos, test que lo fija). **Surfacear, no reinventar**: aprobar preliquidación y registrar anticipo son botones-enlace a los formularios REALES de la ficha (F2.1.c, mismo permiso admin+gerencia — cero flujos duplicados); facturar/pagar/liquidar/cerrar operan in situ. Roles: gerencia opera; admin/gerencia_general lectura (admin ve los enlaces de preliquidación — su permiso real — pero no los botones gerencia-only). Regla: `+'cierre'` al hasOnly + inmutabilidad. E2E: contadores moviéndose sección a sección con las acciones reales, cierre con reconciliación, reglas 7/7, rol de proyectos sin acceso. Tests 190 → **195**.

**Aprobador de respaldo con salvedad — preliquidación** (23-jul-2026, **PR #45**, commit de merge **`fe12a61`**, validado E2E; regla desplegada tras el merge): la titular sigue siendo `gerencia_administrativa` (aprueba sin salvedad); **respaldo controlado**: `gerencia_general`, `gestion_integral` y `admin` aprueban SIEMPRE con **`salvedad` obligatoria** (por qué no aprobó la titular — aplica también a la re-aprobación tras corrección del Bloque 4); los roles de PROYECTOS jamás aprueban (definir y aprobar en la misma área rompe la segregación). UI: botón "Aprobar como respaldo (con salvedad)" → formulario obligatorio; `preliquidacion.salvedad` + traza "aprobada por RESPALDO (rol) — SALVEDAD: …" + **chip ámbar permanente** en la ficha; la titular al re-aprobar limpia la salvedad vieja. **Reglas**: `fijaAprobacionPreliq()` distingue FIJAR un aprobador de RETIRARLO (la reversión del Bloque 4 sigue viva — probado en regresión); gestores no fijan aprobación salvo admin/gerencia_general con salvedad — **BONUS: cierra a nivel de reglas el hueco de que un rol de proyectos aprobara** (hardening por-campo del campo más crítico, antes anotado como futuro); regla nueva de `gestion_integral` con hasOnly acotado + salvedad + estado solo hacia aprobada/anticipo_girado. Notas: `gestion_integral` ahora VE el módulo Proyectos (necesita la ficha); `admin` también aprueba con salvedad (decisión de Giovanny: traza honesta, nadie aprueba sin justificar). E2E: 7/7 con tokens reales incl. "comercial CON salvedad → 403". Tests 195 → **199**.

**Liquidación anticipada al contratista — cierra la tanda de 3 (bandeja → respaldo → anticipada)** (23-jul-2026, **PR #46**, commit de merge **`cd6b7e8`**, validado E2E con regresión completa de la máquina; regla desplegada tras el merge): excepción CONTROLADA — por acuerdo entre Gerencia de Proyectos y Administrativa se paga al contratista ANTES de cobrar. **MÁQUINA DE ESTADOS EXPLÍCITA** hacia `liquidado_contratista`: `pagado_cliente` (NORMAL) · `facturado` (ANTICIPADA: `liquidacion.liquidacion_anticipada=true` + `justificacion_anticipada` + constancia `acuerdo_con`/`acuerdo_fecha`) · **cualquier otro origen PROHIBIDO** — BONUS de endurecimiento: antes cualquier estado con gate podía. Innegociable: gate SST `al_dia` SIEMPRE; nunca antes de `facturado`; solo se salta UN estado. UI: "⏩ Liquidar anticipado" solo en facturado con gate (modal ANTICIPADA con justificación + constancia obligatorias; conciliación idéntica al 3b; nota ámbar en el PDF). **Cobro posterior**: el liquidado-anticipado sigue en "Por cobrar" (predicado `pagoClientePendiente` = facturado O liquidado sin pago); "Registrar cobro pendiente" llena `pago_cliente` SIN cambiar el estado. **INTEGRIDAD del cierre**: no se cierra con cuenta por cobrar abierta (`puedeCerrarseProyecto` en UI + `pago_cliente != null` en regla). Sutileza clave: un doc YA liquidado se sigue editando (el pago posterior) sin re-evaluar la transición. E2E: 7 puntos del spec + batería de reglas 9/9 con regresión del flujo normal. Tests 199 → **206**.

**Cotizador INGEMEC — valor Matriz vs. valor NEG (factor 72%/15% por ítem)** (23-jul-2026, **PR #47**, commit de merge **`62f4c8a`**, validado E2E de punta a punta; SIN reglas ni CF — cambio VISIBLE en producción al mergear, F1 en vivo): el LPU de INGEMEC son los precios de la **MATRIZ** (ATC→INGEMEC), no los de NEG — INGEMEC paga el **72%** (normal) o el **15%** (cuando suministra material); antes el cotizador usaba el full y la oferta + liquidación salían infladas. **LPU**: `esquema_factor` opcional (`types/sigp/lpu.ts`: factor_default 0.72 / factor_alt 0.15 + etiquetas) — solo el LPU de INGEMEC lo lleva (habilitado en prod por write puntual, no hay UI del campo). **Ítem**: `valor_matriz` CONGELADO (patchInstancia lo borra de cualquier patch — fidelidad de snapshot), `factor`/`factor_etiqueta` (lo ÚNICO editable del precio, selector por ítem) y `valor_unitario` = **valor NEG DERIVADO** (`valorNegDe` = Matriz × factor); teclear el precio directo se re-deriva (defensa probada). Como el operativo es el NEG, **calcularTotales, snapshot del proyecto, preliquidación y liquidación heredan el valor correcto SIN tocarse**. UI: celda con Matriz gris + selector 72/15 + NEG. **PDF**: solo con ítems Matriz — columna VR. MATRIZ gris (le resta a DESCRIPCIÓN), VR. UNITARIO → VR. NEG, etiqueta del factor bajo cada descripción (altura contada en la fila), total en NEG; `conMatriz=false` → código idéntico (PDFs de otros clientes byte-iguales, compacción Tanda 1 intacta). E2E: COT INGEMEC 72/15 en vivo (CD $795.000 vs Matriz full $1.500.000, total $946.050) → PDF renderizado a imagen y verificado → aprobada → PRY `valor_venta = NEG`; regresión IHS limpia. Tests 206 → **213**.

**Pipeline automático solicitud → visita → cotización — borradores sin código + badges** (23-jul-2026, **PR #48**, commit de merge **`ff3449c`**, validado E2E completo; SIN reglas/CF/Storage — cambio VISIBLE en producción al mergear, F1 en vivo): la DECISIÓN de la solicitud (y la realización de la visita) crea sola el siguiente pendiente como **BORRADOR SIN código**, precargado y editable. **PRINCIPIO (contigüidad ISO): el consecutivo se asigna al MATERIALIZAR** — la visita al AGENDAR (VIS) y la cotización al DILIGENCIAR (primer guardado con contenido, con preservación del número ante fallo — cero huecos; verificado contador `COT_2026 = 3` exactos); los pendientes cancelados jamás queman número; los formularios manuales no cambian su momento de asignación. **Estados nuevos**: visita `pendiente_agendar` (→ programada/cancelada) y cotización `pendiente_diligenciar` (→ borrador) — máquinas existentes intactas; `estadoEfectivo` no convierte pendientes en vencidas. **Enrutamiento** (`utils/sigp/pipeline.ts`, constructores puros testeados): "requiere visita" → visita borrador `sol_{id}`; "lista para cotizar" → cotización borrador; visita `realizada` → cotización `vis_{id}` precargada (solicitud + visita; campo nuevo `cotizacion.visita_id` de traza). **IDEMPOTENTE en dos capas**: id determinístico + consulta por enlace (una sola por solicitud, incluso si ya existía una manual; re-transición probada → no duplica). **Precarga editable**: la visita abre el form real de Programar precargado y materializa sobre el MISMO doc (historial encadenado `pendiente_agendar → programada`); la cotización pendiente es editable como borrador y materializa al guardar. **Badges del sidebar** (`hooks/sigp/usePendientesSigp`, onSnapshot — patrón del badge de Registros): naranjas, CLICKEABLES → `?pendientes=1` → bandeja con el filtro preseleccionado; "sin código · pendiente" en bandejas y detalles; StatCard "Por agendar". Tests 213 → **224**.

**Columna "Nombre del sitio" + búsqueda en las bandejas de Solicitudes y Visitas** (23-jul-2026, **PR #49**, commit de merge **`9a6a456`**, validado E2E; sin reglas ni datos — visible en producción al mergear): el nombre del sitio (identificador interno principal de NEG, Bloque 1) ahora se muestra en ambas bandejas — columna junto a Cliente ("—" en históricas, truncada con tooltip; descripción contenida). **Búsqueda de texto NUEVA en ambas** (no existía): consecutivo, SITIO, código del sitio, cliente/prospecto, asunto/descripción. Ajuste de raíz: el form manual de visita al vincular solicitud prefiere el `nombre_sitio` canónico (antes copiaba el campo libre viejo `sitio`). Tests 224/224.

**Panel de Marcela — sidebar enfocado + mapa proactivo "En camino"** (23-jul-2026, **PR #50**, commit de merge **`1639583`**, validado E2E con el rol; sin reglas ni datos — visible al mergear): el sidebar de `gerencia_administrativa` queda en 5 ítems (Gestión Administrativa · Obras · Contratistas · Clientes · **Cotizaciones solo lectura**) — fuera Panel/Solicitudes/Visitas/Proyectos; las RUTAS siguen vivas (la bandeja enlaza a la ficha para aprobar preliquidación). Cotizaciones solo-lectura confirmado SIN cambio de reglas (read `accesoSIGP()` ya la incluye; write ya la excluye). **Mapa proactivo** en la bandeja: sección informativa **"En camino"** (pill punteada — los 8 estados de preparación/ejecución con `ETIQUETA_EN_CAMINO` de etapa/qué espera, sin botones; test de cobertura total: todo estado del ciclo cae en acción O en camino) + **resumen NARRATIVO** en una línea (`narrativaAdministrativa`: "Por hacer ahora: … En camino: N." / "Todo al día · N en camino."); "Todo el ciclo" abarca todo lo vivo. E2E: sidebar de 5, narrativa con conteos reales, cotización en solo lectura total, y el salto En camino → Por facturar al hacer el handoff. Tests 224 → **227**.

**Saludo personalizado en Gestión Administrativa** (23-jul-2026, **PR #51**, commit de merge **`f22ce6f`**, solo texto del encabezado — Vercel únicamente): título "Hola, {primer nombre} 👋" del perfil (sin nombre real → "Hola 👋"; guarda: el AuthContext hace fallback nombre=email y un '@' no cuenta como nombre) + subtítulo fiel al proceso real: los soportes se reciben y se da luz verde aquí, **la factura se hace en Siigo**, el SIGP guía etapa por etapa y deja la trazabilidad. Resto del panel (narrativa, pills, "En camino") intacto.

**Modalidad de contratación + indicador ISO #3 honesto** (24-jul-2026, **PR #52**, commit de merge **`f931346`**, trabajado en 4 sub-bloques con OK por sub-bloque y validado funcionalmente por Giovanny en ambos casos; SIN reglas ni functions — solo Vercel): el indicador "Proyección presupuestal" dividía por el valor de VENTA (incluye la utilidad) y todo proyecto rentable salía rojo. **MODELO**: `preliquidacion.modalidad_contratista` (`todo_costo` default | `solo_mano_obra`) + `valor_materiales` (presupuesto de materiales que compra NEG, requerido solo en solo_mano_obra) — radio al DEFINIR, CONGELADOS con la aprobación (spread), **fuera del path de corrección del Hotfix B** (la línea base del presupuesto no se mueve; lo que varía es el costo ejecutado); trazas de definición y aprobación con modalidad+materiales; retrocompat `modalidadDe` → todo_costo. **FUENTE DE VERDAD NUEVA**: `costoPresupuestadoDe` (contratista + materiales si aplica) y `utilidadEsperadaDe`/`margenEsperadoPctDe` (venta − costo presupuestado total, alerta negativa honesta). **Riesgo A**: la palanca margen↔valor conserva su semántica APU (`utilidadDe`/`margenPctDe` intactos, solo reetiquetada "margen sobre el contratista — no es la utilidad real"). **Riesgo B**: anticipo y saldo siguen sobre `valor_contratista` PURO. Ambos riesgos FIJADOS POR TEST. **INDICADOR #3** = Σ ejecutado / Σ costo presupuestado total (misma canasta por modalidad), meta 90–110 intacta; tarjeta "costo/presupuesto"; inline "% del presupuesto"; rótulo del campo de costo ejecutado con canasta inequívoca + tooltip. **PDF del contratista**: solo_mano_obra → "TOTAL MANO DE OBRA" + nota "los materiales los suministra NEG"; todo_costo/históricos byte-idéntico; **no-fuga de `valor_materiales` garantizada por TIPO** (el DTO del PDF no tiene el campo). Tests 227 → **239** (ind3 con 4 casos incl. **>100 % legítimo documentado como resultado de negocio**, agregado mixto, matriz de modalidades, utilidad negativa real). **PENDIENTE FUERA DEL REPO**: reflejar la definición nueva del indicador en la **Caracterización v02** con aval de **Ingrid (GI)**.

**Importador de LPU — detección robusta de unidad + guardia bloqueante** (27-jul-2026, **PR #53**, commit de merge **`70bdc4d`**, trabajado en 3 sub-bloques con OK por sub-bloque y validado funcionalmente por Giovanny; SIN reglas ni functions — solo Vercel): la hoja "4. OBRAS CIVILES" de la Matriz ATC titula la columna de unidad **"UN" a secas** y el regex de `sugerirMapeoColumnas` no la veía → **153 ítems entraron a producción con `unidad: ""` en silencio** (auditoría 26-jul; único consumidor afectado COT-2026-017 → PRY-2026-002 — su backfill es tarea aparte, "Prompt C" pendiente). **DETECCIÓN** (`lpuMapeo.ts`): `esEncabezadoUnidad()` — formas cortas (`UN`/`UND`/`UM`/`U.M.`/`U/M`/`UNID`) por **match exacto ANCLADO** sobre el encabezado normalizado (mayúsculas, sin puntos/espacios; jamás substring — cero falso positivo con "VALOR UNITARIO"/"UNIT PRICE"); largas por regex (incluye H-009 inglés). **GUARDIA post-import** (`consolidar` + wizard): `itemsSinUnidad` + `ejemplosSinUnidad` (hasta 6, `código — descripción [hoja]`) + `requiereConfirmacion` (unidades vacías O códigos duplicados) → panel rojo con ejemplos y pista de mapeo, checkbox **"Forzar importación" consciente**, Siguiente/Importar BLOQUEADOS sin él, y el forzar **se resetea al cambiar mapeos u hojas** (una confirmación vieja no autoriza datos nuevos). Tests 239 → **247** (detección positivos/negativos, regresión con el encabezado real de la hoja 4, guardia completa). Validado E2E con el Excel real: la hoja 4 auto-mapea `UN`; la guardia aflora 17 sin unidad reales de hojas auxiliares (9. AIU/MEMORIAS) + duplicados 1.3.7/3.5.18, y el escenario exacto del bug (desmapear unidad → 170) queda bloqueado.

**Regla unificada: la solicitud pasa a «cotizada» al ENVIAR la cotización** (27-jul-2026, **PR #54**, commit de merge **`973cf04`**, validado por Giovanny; SIN reglas ni functions — solo Vercel): bug reportado por Giovanny en prod — SOL-2026-016 (Plaza San Nicolás) congelada en «lista para cotizar» con su COT-2026-018 APROBADA y PRY-2026-011 EN EJECUCIÓN. Causa: la materialización del pipeline (PR #48) jamás transicionaba la solicitud (la transición cruzada solo vivía en el formulario manual, y demasiado pronto: al crear el borrador vacío). **REGLA (decisión de Giovanny)**: la solicitud transiciona SOLO cuando la cotización queda EN FIRME — al ENVIAR — en ambos caminos. Helper puro `patchSolicitudCotizada()` en `pipeline.ts` (patch solo desde `lista_para_cotizar`, null en el resto → sin doble transición en reenvíos/nuevas versiones), aplicado en `CotizacionAcciones.enviar` (no-fatal); transición temprana del formulario RETIRADA (write-back del asunto intacto); **selector de «Nueva cotización» excluye** solicitudes con cotización enlazada (ahora siguen en lista mientras su cotización es borrador) + aviso — evita duplicadas. Tests 247 → **250**. E2E completo del flujo + guarda anti-duplicado. **Reparación de datos AUTORIZADA ejecutada en prod** (write puntual con pre/post-verificación): SOL-2026-016 → `cotizada` con historial honesto referenciando COT-2026-018 y la autorización; SOL-2026-017 (Santa Helenita) se corrige sola cuando envíen COT-2026-020 (post-merge).

**Reparación de datos AUTORIZADA: unidades del LPU INGEMEC + duplicado 3.5.18 + snapshot COT-2026-017** (27-jul-2026, sin PR — solo datos en prod, flujo dry-run → OK explícito → aplicación por subagente Sonnet con pre/post-verificación por documento; cierra la deuda del bug del importador arreglado en PR #53): **ORÁCULO** `mapa_unidades_INGEMEC_ATC.json` (640 códigos → unidad LITERAL) generado desde el Excel ORIGINAL de la Matriz guardado en Storage (`archivo_original` del import). **1)** LPU `lpus/2ext9AunVnACw9U0mLHx`: los 153 ítems de "4. OBRAS CIVILES" con `unidad: ""` rellenados por código contra el oráculo (153/153 match, commit ATÓMICO, updateMask solo `unidad`; distribución m2×58 · m×38 · Und×24 · m3×19 · Glb×11 · ml/Kg/und×1 — literales del Excel, fidelidad) → **0 unidades vacías en 642 ítems**, spot-check 5/5. **2)** Duplicado 3.5.18 (viene del Excel mismo, ambos en ESTRUCTURAS): `zIQ0zztyZYe2MHnaBlEE` "Ajuste de Verticalidad" (Glb) renumerado a **3.5.19** (libre en LPU y Excel — verificado); `MLHOHRr1kS2p4qFgUVI1` "Convertidor de óxido." (gal) conserva 3.5.18 (coincide con la 1ª ocurrencia del Excel). **3)** COT-2026-017 `versiones/1` (snapshot congelado, backfill autorizado): las 6 unidades del alcance (4.1.1/4.1.5→m2, 4.1.2→Glb, 4.3.7/4.3.10/4.11.14→m2) escritas reconstruyendo el array `items` con TODO lo demás byte-idéntico + **traza en el historial del padre** (motivo completo con autorización). **HALLAZGO de alcance**: `snapshot.alcance` del proyecto es resumen por grupo (sin unidades por ítem) — el PDF del contratista lee la versión aprobada EN VIVO, así que reparar `versiones/1` bastó. **CIERRE**: PDF del contratista de PRY-2026-002 re-renderizado con datos reales de prod (harness vite-node + réplica exacta de `generarDocContratista`) — columna UND completa, total $474.936 intacto. La LPU no tiene campo de historial → la traza a nivel LPU es esta entrada (sin tocar estructura, premisa del prompt).

**La solicitud pasa a «aceptada · proyecto creado» al aprobar la cotización** (27-jul-2026, **PR #55**, commit de merge **`6a2d343`** — el PR se cerró SIN merge dos veces por accidente en la UI de GitHub; rama restaurada/reabierta en ambas y merge final ejecutado por Code vía `gh pr merge` con autorización explícita de Giovanny; SIN reglas ni functions — solo Vercel): continuación del PR #54 — la solicitud comercial moría en «cotizada» aunque la cotización se aprobara y el proyecto corriera (reporte de Giovanny: Plaza San Nicolás y Tesoro III). Helper puro `patchSolicitudAceptada()` en `pipeline.ts` (a `aceptada` — el terminal que ya usaban los preventivos, ahora compartido — desde `cotizada` o `lista_para_cotizar` pre-#54; null en el resto → reintentos idempotentes), aplicado en `crearProyecto` de `CotizacionAcciones` (cubre aprobación y el botón de reintento, que además repara enlaces viejos; no-fatal). `aceptada` sigue siendo SOLO programática (sin clic genérico). Tests 250 → **254**. **Reparación de datos AUTORIZADA ejecutada aparte en prod** (write por subagente Sonnet, pre/post-verificación): SOL-2026-006, SOL-2026-007 (Tesoro III) y SOL-2026-016 (Plaza San Nicolás) → `aceptada` con historial honesto referenciando su COT/PRY y la autorización.

**Selector de orden en la bandeja de Gestión Administrativa** (27-jul-2026, **PR #56**, commit de merge **`b6f94f4`**, diseño aprobado → implementación por subagente Sonnet → validado E2E y funcionalmente por Giovanny; SIN reglas ni functions — solo Vercel): Marcela no podía ordenar los proyectos dentro de la bandeja. Selector en la cabecera (junto al buscador): **Más reciente** (default, `fecha_actualizacion ?? fecha_creacion` desc) · **Más antiguo** (asc) · **Cliente A–Z** (`snapshot.cliente`, `localeCompare` es/base — insensible a mayúsculas/tildes — desempate por reciente). La bandeja es una tabla con pills: en una sección concreta el orden va directo; en "Todo el ciclo"/"En camino" el agrupado por etapa sigue como criterio primario y el orden actúa DENTRO de cada etapa. Preferencia en `localStorage` (`sigp.admin.orden`) — sobrevive al cierre del navegador (decisión de Giovanny). Client-side puro; conteos/narrativa/consultas intactos. Un archivo (`FacturacionPagos.tsx`).

**MÓDULO COMPRAS · C1 — Registro de proveedores** (02-ago-2026, **PR #61**, commit de merge **`5ff4185`**, diseño en sub-bloques con OK + implementación UI por subagente Sonnet + validación funcional de Giovanny; **reglas Firestore + Storage DESPLEGADAS en prod el 02-ago** con pre-verificación prod==HEAD CRLF/LF-normalizada — diff exacto +42/−0 y +15/−0, solo C1 —, post-verificación byte-idéntica y denegación anónima 403): los materiales los compra NEG; Marcela registra proveedores con RUT + certificación bancaria. **MODELO**: `proveedores/{id}` (doc id = identificación NORMALIZADA `normalizarIdentificacion`; identidad + `rut_url` + contacto + estado soft-delete sin delete) + subcolección **`privado/datos_bancarios`** (banco/tipo/número de cuenta + `certificacion_bancaria_url`) — las reglas no ocultan campos en lectura → lo bancario vive en doc aparte SOLO gerencia+admin (principio verificaciones_sst); `contactos_lookup` solo-lectura (el seed de 633 se RETIRÓ del alcance — wizard manual futuro; la UI degrada silenciosa si está vacía). **REGLAS**: helper `gestionaCompras()` (gerencia_administrativa+admin); read identidad = accesoSIGP (para C2); create exige `rut_url`; create de privado exige cert+banco+cuenta. **STORAGE**: `proveedores/{id}/{rut|certificacion_bancaria}/{uuid}` — scoping + contentType pdf/imagen + <10MB; filename UUID como mitigación H-008 (rol no validable en Storage, documentado). **ALTA ATÓMICA por `runTransaction`** (corrección de Giovanny): tx.get aborta si el id existe — un set sobre doc existente sería update permitido — + tx.set de ambos docs (jamás proveedor sin bloque bancario ni pisado por carrera). UI `/administrativa/proveedores` (sidebar ADMINISTRATIVA solo gerencia+admin): tabla+búsqueda+filtro, alta con autocompletado del lookup, id normalizado en vivo con chequeo de duplicado, validación dura, edición identidad/contacto, modal bancario, toggle estado. Tests → **292** (14 nuevos). E2E completo + matriz de reglas 8/8 con tokens reales de 3 roles. NOTA de convivencia: los **PRs #57–#60** (mejoras SST + endurecimiento de reglas de `users`, de sesiones paralelas — sus reglas desplegadas el 30-jul) entraron a main durante C1; los bloques C1 son inserciones puras encima, y `vitest.config.ts` ahora excluye `.claude/worktrees/**` (checkouts de otras sesiones contaminaban la suite).

**Codificación ISO en el PDF de preliquidación del contratista** (02-ago-2026, **PR #62**, commit de merge **`2f126a0`**, diseño→diff→OK→validación con renders; solo frontend — Vercel): el documento del contratista no llevaba control documental. **`utils/sigp/isoControl.ts` (nuevo)**: constante centralizada `PRELIQUIDACION` (GESTIÓN DE PROYECTOS · **SGI-FT-PRL-26** · v01 · AGO-2026 · nombre oficial) + interfaz `ControlDocumentalIso` — primer paso para sacar los códigos ISO de literales por archivo; el `CM-FT-CT-19` de cotización se migrará en tarea aparte (ese PDF no se arriesga). **Página 1**: cuadro ISO réplica exacta del de cotización (3 columnas, Montserrat, todo en negro, logo color), insertado con el patrón `y = yIso - 8`; el centro usa `ISO.area`/`ISO.nombre` de la constante (mejor que el hardcode de cotización). **Páginas 2+**: corrido con `PRELIQUIDACIÓN · SGI-FT-PRL-26 · v01` (decisión de Giovanny: nombre + código juntos). "Página X de Y" intacto. Barra TOTAL CONTRATISTA sin riesgo (bloque relativo). Validado con render real de PRY-2026-002 (1 página, mismo total) + sintético de 4 páginas. **PENDIENTE FUERA DEL REPO (frente Trinorma)**: ratificación de `SGI-FT-PRL-26` por **Ingrid (GI)** + registro en el **Listado Maestro** — anotado también en isoControl.ts.

**MÓDULO COMPRAS · C2 — Órdenes de compra** (02-ago-2026, **PR #63**, commit de merge **`4bfb77c`**, diseño en sub-bloques con OK + UI por subagente Sonnet + E2E de 4 roles; **Functions Y reglas Firestore/Storage DESPLEGADAS en prod el 02-ago 22:46 UTC** — CF `generarConsecutivo` actualizada con prefijo `OC` verificada por functions:list SIN quemar número (el primer OC real del equipo tomará OC-2026-001); reglas con pre-verificación prod==HEAD CRLF/LF (+59/−0 FS, +10/−0 ST — puro C2), post byte-idéntica y denegación anónima 403): la OC la crea un operativo (**auxiliar_proyectos/director_proyectos/gerencia_general** — `residente_obra` SE SUMARÁ a puedeCrearOC/puedeVerOC cuando exista el rol) con la **cotización del proveedor adjunta**, y la aprueba GP/GG con **aprobador ≠ creador o salvedad obligatoria** (escape GG-crea/GG-aprueba — exigido A NIVEL DE REGLAS, no solo UI). Documento de EJECUCIÓN: costo operativo, jamás venta/utilidad. **MODELO** (`ordenCompra.ts`): consecutivo al EMITIR (contigüidad ISO — borradores no queman número, preservación ante fallo), `proveedor_snapshot` copiado (jamás toca `privado`), líneas con total derivado, `borrador→emitida→aprobada` + `anulada` soft sin delete; `aprobada` inmutable salvo anulación POR APROBADOR; matriz de anulación por estado. **REGLAS**: `puedeCrearOC`/`apruebaOC`/`puedeVerOC` (lista explícita mínimo-privilegio: sin comercial ni sst, con gestion_integral) + `fijaAprobacionOC` (patrón preliquidación); nace borrador con creador real no suplantable; emitir exige adjunto. **UI**: sección en la ficha (crear/editar/adjuntar UUID/emitir/aprobar con modal de salvedad/anular con motivo) + bandeja `/sigp/ordenes-compra` (pills con contador, default "Por aprobar"; filas enlazan a la ficha — surfacear, no reinventar) + sidebar/ruta por rol; render DEFENSIVO ante docs malformados (fix propio sobre lo de Sonnet). Tests → **303** (11 nuevos). E2E: OC-2026-001/002 en emulador, salvedad end-to-end, **batería de reglas 10/10 con tokens reales** (incl. los 2 casos explícitos de Giovanny: GG crea 200; gerencia_administrativa crea 403/lee 200). **Siguiente: C3** — la compra de Marcela + auto-agregado que alimenta el indicador presupuestal con compras reales (consume el estado `aprobada`).

**MÓDULO COMPRAS · C3 — Compra de Marcela + auto-agregado al indicador presupuestal** (03-ago-2026, **PR #64** commit de merge **`d825f4f`** + **PR #65 fix `32192b3`**, diseño en sub-bloques con OK + UI por subagente Sonnet + E2E con el trigger VIVO; **2 CFs nuevas + reglas Firestore/Storage DESPLEGADAS en prod el 03-ago 05:12 UTC** — `sincronizarComprasProyectoDesdeOC`/`DesdeMenores` verificadas por functions:list SIN fabricar compras; reglas con pre-verificación CRLF/LF, post byte-idéntica y denegación anónima 403 sobre compras_proyecto y la vía de compra): al marcar una OC **`comprada`** (o registrar una **compra menor**), la CF mantiene la proyección **`compras_proyecto/{id}`** y el **indicador #3 por fin se alimenta de compras REALES**. `comprada` = aprobada→comprada SOLO gestionaCompras (hasOnly + valor_real>0 + soporte UUID), TERMINAL (recepción/devolución = v2) **pero con vía de CORRECCIÓN de monto** (comprada→comprada, hasOnly, motivo trazado — filosofía Hotfix B: dinero real con typo no queda inarreglable). Proyección patrón verificaciones_sst: solo la CF escribe (Admin SDK), read **puedeVerOC** (corrección de confidencialidad de Giovanny: el agregado no filtra por la puerta de atrás lo que C2 cerró — Karen/comercial dejó de ver el KPI, degrada a "—" con nota). CF: 2 triggers convergentes, recálculo-desde-cero idempotente, compare-before-write, jamás escucha `proyectos`, sin índice compuesto. **`costoEjecutadoDe` con GATE POR ESTADO** (criterio ISO: el cumplimiento presupuestal se evalúa desde `ejecutado`; manual histórico GANA siempre — sin doble conteo ni reescritura del pasado); campo manual → solo lectura con desglose "mano de obra + compras = total · derivado automático". `compras_menores` subcolección propia (gestionaCompras, valor>0, sin delete) + tarjeta en ficha. Bandeja: pills Por comprar/Compradas + acciones Comprar (valor prellenado + advertencia si difiere) y Corregir compra (motivo obligatorio). Tests → **313**. E2E: comprar por UI → agregado se materializa SOLO (315k) → menor suma (435k) → Panel 97,4% En meta → corrección a la baja (400k) con estado intacto → comercial "—"; baterías de reglas 15/15 y 9/9 post-fix. **⚠ INCIDENTE DE CONVIVENCIA (PR #65)**: una sesión paralela SOBRESCRIBIÓ firestore.rules en el working tree entre el E2E y el commit — el #64 llevó el hardening de `formularios/revision_sst` de esa sesión (anti-auto-aprobación del técnico, quedó en main y SE DESPLEGÓ con este cierre; esa sesión debe validarlo como suyo) y CERO bloques C3. Lo atrapó la pre-verificación del deploy (diff prod→HEAD sin C3) ANTES de tocar prod; bloques restaurados exactos en #65 con re-batería 9/9. **Lección operativa**: antes de commitear reglas, verificar que el diff a commitear contenga exactamente los bloques propios (no solo "modified"); y evitar sesiones paralelas editando reglas simultáneamente (la sesión SST quedó pausada). **PENDIENTES**: Caracterización v02 con el gate del #3 (Ingrid/GI, frente Trinorma); recepción/devolución = Compras v2; `residente_obra` a los helpers OC cuando exista el rol.

**MÓDULO COMPRAS · C4 — Conciliación de reembolsos en el indicador** (03-ago-2026, **PR #66**, commit de merge **`e9f126a`**, reporte→diseño→OK→implementación Sonnet→validación; SIN reglas/CF/Storage — solo Vercel): el derivado de C3 ignoraba los **reembolsos del contratista** (`compras_reembolsos`, línea del 3b — array VIVO en la raíz de `proyectos`, congelado al liquidar e idéntico al snapshot de liquidación). **`costoEjecutadoDe`** suma ahora el tercer componente DENTRO del helper (callers sin cambio de firma): `valor_contratista + compras_ejecutadas_total + totalComprasReembolsos(...)` — sin doble conteo por diseño (líneas disjuntas: OCs/menores = NEG compra; reembolsos = el contratista compró y NEG reconoce), manual histórico gana ÍNTEGRO, gate por estado intacto, retrocompat +0. Desglose de la ficha con **los tres términos siempre** ($0 explícito — más auditable); fuente de la tarjeta #3 → "(OCs + menores + reembolsos)"; **degradación de comercial intacta** (un #3 "solo con reembolsos" serían valores a medias). Sin cambio de reglas (`compras_reembolsos` viaja en el doc `proyectos` que los lectores ya leen — y quedó ANOTADO EL HALLAZGO §16: comercial lee el doc completo de proyectos incl. preliquidación, fisura PREEXISTENTE de F2; endurecimiento arquitectónico aparte con patrón sub-doc confidencial, a revisar con Giovanny e Ingrid). Tests → **317**. Verificado en emulador: desglose $2.000.000 + $705.000 + $200.000 = $2.905.000 · 116,2% y Panel "Sobre meta" — la conciliación destapando justo lo que buscaba. **CANASTA DEL INDICADOR COMPLETA: mano de obra + OCs + compras menores + reembolsos.**

**Comercial #1 — decisión de rumbo al registrar la solicitud** (07-ago-2026, **PR #68**, commit de merge **`4cda7df`**, reporte→diseño→OK (variante de TRES opciones, decisión de Giovanny)→implementación→E2E; solo frontend — Vercel): toda solicitud obligaba dos pasos (registrar en `recibida` → detalle → estudio → decidir). Ahora **nace en `en_estudio`** (historial con motivo de registro; `recibida` queda para históricas/corrección) y el formulario ofrece el radio **"¿Qué sigue?"** (OBLIGATORIO sin default, solo comerciales — preventivos intactos): *Requiere visita técnica* → transiciona en el acto + borrador de visita del pipeline §48 · *Cotizable inmediata* → `lista_para_cotizar` + borrador de cotización · *Decidir después* → queda en estudio (botones del detalle). Historial con los DOS eslabones (registro → en_estudio → decisión) — la máquina no se salta nada; máquina/pipeline/detalle/reglas SIN tocar; reversible. Helper puro `historialRegistroCon()` + 4 tests → **321**. E2E de los 3 caminos + obligatorio bloqueando. NOTA de convivencia: la implementación apareció duplicada en el working tree por ejecución paralela del mismo prompt — origen del adopto de la **práctica de worktrees por carril** (documentada en cap. 10).

**Indicador operativo de margen real con meta configurable — Opción B** (07-ago-2026, **PR #69**, commit de merge **`51418e5`**, PRIMER bloque desarrollado íntegramente en WORKTREE propio (`wt-indicador-margen`) según la práctica del cap. 10; **regla `configuracion/{docId}` DESPLEGADA en prod el 07-ago 03:37 UTC** — pre +8/−0 exacto, post byte-idéntica, denegación anónima read/write 403): tarjeta **"💰 Margen real"** (chip "operativo" — NO es indicador ISO oficial) en la capa operativa del tablero: margen agregado = Σ(venta pactada − costo ejecutado derivado)/Σventa — `utilidadRealDe`/`indMargenReal` heredan de `costoEjecutadoDe` el gate desde `ejecutado`, el manual histórico y la canasta completa (C3/C4). **Semáforo más-alto-es-mejor**: verde ≥ meta · ámbar "Bajo meta" [meta−5, meta) · rojo **"Muy bajo meta"** (label propio, decisión de Giovanny — sin depender solo del color; solo en esta tarjeta, el pillDe de las oficiales intacto); **sin meta → % sin pill**. **Meta configurable**: doc `configuracion/indicadores {meta_margen_pct}` — regla read `accesoSIGP` / write SOLO gerencia_general+admin / sin delete; edición inline en la tarjeta con traza (helper `editaMetaIndicadoresUI`). Degradación de comercial IDÉNTICA al presupuestal ("—" + nota; ve la meta, sin ⚙). Tests → **328**. E2E desde el worktree: 14,24% exacto, 3 bandas con labels/colores, batería de la regla 8/8. **MECÁNICA DE WORKTREE completada para futuras sesiones** (aprendida aquí): tras `git worktree add`, (1) `npm install` en la raíz Y en `functions/` del worktree, (2) copiar `.env` y `.env.local` (gitignoreados — no viajan con el checkout; sin ellos el panel muere con auth/invalid-api-key), (3) emulador y vite (`npx vite --port 5175`) se corren DESDE el worktree para que el E2E use sus reglas/código, (4) al cerrar: matar los procesos del worktree ANTES de `git worktree remove` (los handles bloquean el borrado; un directorio residual ocupado es cosmético y se limpia al reiniciar), (5) `functions/package-lock.json` modificado por el install local NO se comitea.

**§16 (ii) — Nacimiento del proyecto SERVER-SIDE + comercial FUERA de `proyectos`** (07-ago-2026, **PR #70**, commit de merge **`715590d`**, desarrollado en worktree `wt-16-proyectos` por sub-bloques SB1–SB4 con OK por cada uno; **cierre autónomo autorizado con orden de deploy CONTRACTUAL: CF → Vercel → reglas, ejecutado el 07-ago 06:10–06:13 UTC**): cierra la fisura §16 (comercial leía el doc completo de `proyectos`, incl. preliquidación) bajo el principio **"la frontera de comercial es la COTIZACIÓN; los proyectos son dominio de ejecución"**. **CF `crearProyectoAlAprobar`** (`functions/crearProyecto.js`, 2 triggers: `cotizaciones` en transición real →`aprobada` y `solicitudes` tipo preventivo →`aceptada`, con guarda de tipo anti-doble-disparo y escotilla = re-tocar el doc sin `proyecto_id`) con **enfoque STAGING elegido sobre el port TS→JS**: el cliente construye el snapshot con los builders TS de siempre y lo escribe como `snapshot_proyecto` EN EL MISMO updateDoc de la aprobación/aceptación; la CF lo **COPIA** (jamás reconstruye) en **UNA transacción atómica** (contador `PRY_{año}` + set proyecto + enlace inverso + solicitud→`aceptada` replicando `patchSolicitudAceptada` con `por:'sistema'` — la TS se ELIMINÓ, implementación única). Guardas: exige `snapshot_proyecto` (histórico sin staging → warn, no crea); sanity `valor_venta == totales.total` de la versión aprobada (preventivo: `>0` + alcance no vacío — la matriz IHS no se porta); **idempotencia por doc-id** con vía de reparación de enlaces sin consumir contador; `creado_por` = quien aprobó/aceptó. **Creación client-side RETIRADA** (`utils/sigp/proyectos.ts` eliminado; `useConsecutivo('PRY')` fuera; `construirSnapshotPreventivo` puro quedó junto a la matriz). **Reglas**: helpers `puedeVerProyectos()` (6 roles) y `puedeCrearProyectos()` (4, sin comercial); `proyectos` read→`puedeVerProyectos`, create/update(vía operativa)/**delete**→`puedeCrearProyectos` — el **delete lo atrapó la batería SB4** (seguía con la lista vieja CON comercial: podía borrar a ciegas sin poder leer; fix ratificado por Giovanny). **UI**: `ROLES_VE_PROYECTOS`/`veProyectosUI` fuente única espejo de la regla; ruta y sidebar fuera para comercial (`redirectTo=/sigp/panel` anti-bucle); `puedeGestionarProyectosUI` = espejo de `puedeCrearProyectos` (efecto §16 asumido: comercial pierde el "Registrar manual" del ind. 5); **chip PRY reactivo** vía hook `useNacimientoProyecto` (onSnapshot: "⏳ Creando proyecto…" → "creado ✓" sin recargar, timeout suave 20 s habilita reintento; para comercial es SPAN informativo no-navegable); **Panel de comercial sin `getAll('proyectos')`** — inds 1/2/4, dona y tiles IHS degradan con "Requiere acceso a proyectos", el embudo comercial se conserva (tramo proyectos "—"); los 6 roles cero cambio. Tests **328 → 339**; **batería de reglas 39/39** con idTokens reales de 8 roles (comercial 403 × get/list/create/update/delete; staging de comercial en cotizaciones 200; sst 403 en proyectos con su cola viva; anónimo 403); **E2E de oro 22/22** con CFs vivas (snapshot byte-idéntico, re-touch idempotente, reparación sin quemar contador, 3 guardas negativas con traza) + pase de navegador con ambos roles. **DEPLOY contractual verificado paso a paso**: (1) CFs primero — `functions:list` con las 2 nuevas vivas y las 5 existentes re-desplegadas idénticas; inercia probada con foto de prod (contador 14/proyectos 14 intactos; 6 aprobadas con 0 staged → guarda activa); (2) merge #70 verificado `MERGED` real → Vercel bundle `index-DPeb4zqs.js` con los 2 marcadores (`snapshot_proyecto` + chip) confirmados contra la línea base `index-CcrDZCNm.js`; (3) reglas AL FINAL — pre diff prod→HEAD = 32 líneas exactas solo §16 (CRLF/LF normalizado), post **byte-idéntica**, denegación anónima get+list 403; la matriz de roles en vivo queda respaldada por el ruleset byte-idéntico al validado 39/39 en emulador (no se inicia sesión con cuentas reales de prod — disciplina).

**Codificación ISO en el PDF de liquidación del contratista** (07-ago-2026, **PR #71**, commit de merge **`c10da93`**, desarrollado en worktree `wt-iso-liquidacion`; reconocimiento→diff→OK→renders→validación; solo frontend — Vercel): calco 1:1 del PR #62. **Hallazgo del reconocimiento previo**: el "Item 5" pedía ISO en el PDF de VISITAS, que **no existe** (las visitas no generan PDF — su evidencia es checklist/fotos/plano en pantalla); el único generador sin control documental era la LIQUIDACIÓN → Giovanny eligió la Lectura A (el informe de visita quedó como posible bloque futuro con diseño propio). Entregado: constante **`LIQUIDACION`** en `isoControl.ts` (**GESTIÓN ADMINISTRATIVA** — primera con área ≠ Proyectos, coherente con el 3b — · **SGI-FT-LIQ-26** · v01 · AGO-2026); cuadro ISO en página 1 de `liquidacionPdf.ts` (réplica exacta, `y = yIso − 8`, logo color embebido +1 línea); corrido 2+ `LIQUIDACIÓN DEL CONTRATISTA · SGI-FT-LIQ-26 · v01` (7pt gris, reemplaza la palabra suelta). "Página X de Y", pie, totales, sello de conciliación y barra SALDO intactos. Validado con renders: 1 página (igual a la preliquidación) y multipágina (30 actividades + compras + retenciones + anticipada + ajustes + sobre-giro). Diff +48/−1 en 2 archivos exactos. **Con esto los TRES documentos entregables llevan control documental** (CM-FT-CT-19 · SGI-FT-PRL-26 · SGI-FT-LIQ-26). Gotcha operativo nuevo: en PowerShell 5.1 un `git commit -m @'…'@` con comillas dobles INTERNAS rompe el paso de args a git (el commit no se crea aunque el push "funcione") — mensajes multilínea SIEMPRE por `git commit -F archivo`. **PENDIENTE FUERA DEL REPO (frente Trinorma)**: ratificación de `SGI-FT-LIQ-26` por Ingrid (GI) + registro en el Listado Maestro, junto con `SGI-FT-PRL-26`.

**MÓDULO #3 — Validador de horario / asistencia** (09-ago-2026, **PR #72**, commit de merge **`036a77c`**, desarrollado en worktree `wt-horario` por sub-bloques SB1–SB4 con OK por cada uno; cierre autónomo autorizado con **orden de deploy INVERTIDO al del §16 — reglas ADITIVAS primero → CF → merge/Vercel** (aquí las reglas solo conceden, no cierran: sin ventana rota); reglas FS+ST desplegadas 09-ago 03:18 UTC con diff exacto de los bloques del validador (+57 FS / +13 ST, línea base prod==main verificada), post byte-idéntica y anónimo 403 × 5 rutas; CF verificada por functions:list e **inerte** hasta el bundle nuevo — 0 registros en prod pre-merge): filosofía **MARCAR, no bloquear**. **CF `registrarEventoHorario`** (callable v2, patrón generarConsecutivo): el cliente SOLO dice `ingreso`/`salida` — IP (primer salto de `x-forwarded-for`), `dispositivo` (user-agent server-side: móvil/escritorio/desconocido), **timestamp `serverTimestamp()`** (inmune a la hora del navegador) y **`en_oficina` SOFT** (IP exacta o CIDR IPv4 —matcher propio— contra `configuracion/horario.ips_oficina`; **sin config O sin IP determinable → `null`**, jamás un falso "remoto") + `nombre`/`rol` denormalizados de `users`. **Eventos PLANOS** en `registros_horario` (Admin SDK; regla `create/update/delete: if false` — nadie corrige el reloj; read = `veHorario()`: las 4 gerencias + admin); el par ingreso↔salida y los "sin salida" son INTERPRETACIÓN de lectura (`aparearJornadas` puro en `types/sigp/horario.ts` — doble ingreso, salida huérfana, multi-día; orden por el evento más reciente). **Enganche no-fatal** en `login()`/`logout()` del AuthContext (salida ANTES del signOut; `onAuthStateChanged` intacto — cero falsos ingresos por F5; el fallo de la CF jamás bloquea). **Bandeja `/administrativa/horario`** (3er ítem del grupo ADMINISTRATIVA — el grupo ahora abre con `veFacturacionUI || veHorarioUI`, cada ítem con su gate): jornadas apareadas con duración + chips 🏢/🌐/— y 🖥/📱 + "Sin salida" (días previos) + "en jornada" (hoy); **Ausentismos** (incapacidad/permiso/justificación/vacaciones/otro) con **soporte OBLIGATORIO** (Storage `ausentismos/{id}/soporte/{uuid}`, patrón H-008) + **detalle médico en sub-doc `privado/detalle` de LÍNEA DURA** (dato de SALUD: solo gerencia_administrativa+admin — ni GG lo lee, patrón datos_bancarios) + anulación soft con motivo, sin delete; config inline IPs+hora fin (GG+admin, patrón meta de margen). **Pop-up "no olvides cerrar sesión"** para TODOS los roles al pasar `hora_fin_jornada` (hora local, una vez por día vía localStorage, botón directo de logout = la marca de salida) — para eso el **match nuevo `configuracion/horario` read `isAuthed()`** (ratificado; write sigue GG+admin; `indicadores` conserva accesoSIGP — probado 403 para sst). Helpers `ROLES_VE_HORARIO`/`veHorarioUI` (las 4 gerencias+admin, decisión Giovanny) y `puedeGestionarHorarioUI` (gerencia_administrativa+admin), espejo exacto de `veHorario()`/`gestionaHorario()` de reglas. Tests **339 → 352** (apareo completo, `esHoraValida`, heurísticas de la CF importadas del archivo REAL con deps mockeadas, matriz de permisos); **batería 42/42** con idTokens de 8 roles (+ Storage validado con `uploadBytes` real — el POST REST crudo da falso 403 también contra proveedores: artefacto del método, documentado); **E2E navegador 3 roles** con par apareado EN VIVO (login Marcela 22:01 → logout 22:03 → jornada 2 min vía CF). **GOTCHAS de emulador nuevos (mecánica de worktree)**: el emulador de functions puede fallar la carga con "Timeout after 10000" → `FUNCTIONS_DISCOVERY_TIMEOUT=120`; NO hace hot-reload de cambios en CFs (reiniciar); las callables llegan por pipe interno SIN IP (sin XFF localmente → por eso la guarda `sin IP → null`). **Identity Platform / `beforeSignIn` queda DESCARTADO por diseño** (el proyecto está en FIREBASE_AUTH clásico, `blockingFunctions` vacío — verificado por API): solo se habilitaría en consola si algún día se quisiera BLOQUEAR logins, no marcar. Nota operativa: recargar el panel tras el deploy — las marcas arrancan con el bundle nuevo.

**#2b — Mantenimiento de precios del catálogo NEG** (09-ago-2026, **PR #73**, commit de merge **`429dca5`**, desarrollado en worktree `wt-catalogo` (diseño Sonnet revisado → SB1 modelo/reglas → SB2 UI Sonnet con diff verificado → SB3 tests/batería/E2E); **cierre autónomo autorizado con orden §16 — merge/Vercel PRIMERO, regla RESTRICTIVA al final** (comercial/auxiliar tenían update/delete teóricos vía `puedeGestionarProyectos()`); **regla desplegada 09-ago 04:42 UTC** — pre diff prod→HEAD = 18 líneas exactas solo #2b, post byte-idéntica, anónimo GET/PATCH 403): **historial de precios trazable** para `catalogo_items`. **Modelo**: `historial_precios[]` APPEND-ONLY (`{valor_unitario, vigente_desde, actualizado_por, motivo}` — solo el valor NUEVO, el anterior se infiere del elemento previo; **el primer elemento se SIEMBRA al incorporar desde el cotizador** con el COT de origen — todo precio nace trazable; docs legacy sin el campo → chip "ítem anterior a esta función", su primera corrección lo inaugura). **Reglas**: helper `puedeMantenerCatalogo()` (**GG + director_proyectos + admin**); create sigue abierto al cotizador (su alta es `setDoc` sobre doc NUEVO — verificado en código y batería); **update todo-o-nada** para los 3 (se descartó la protección por-campo: hoy ningún flujo de cliente updatea — YAGNI, más simple de auditar); **delete ELIMINADO** (ajuste mío ratificado: soft por `estado`, restricción 5.1 — antes existía delete teórico). **UI** (`/sigp/catalogo`, ítem "Catálogo NEG"; lectura para los 7 roles SIGP, fuera del sidebar enfocado de Marcela): tabla + búsqueda + toggle inactivos + última actualización con motivo + **historial expandible para todos**; **corrección puntual** (InputExpresion + motivo obligatorio) y **ajuste masivo por %** (todo-activo o checkboxes — `CatalogoItem` no tiene categoría; preview actual→nuevo; rango −95/+500; **confirmación extra si |%|>50**; `Math.round` a peso; lotes de 450 espejo del wizard LPU; motivo compartido); lógica pura en `utils/sigp/catalogoAjuste.ts`. **Fix de producto destapado por el E2E**: el modal de corrección prefillaba el precio vigente → un guardar distraído habría creado historial-ruido con el mismo valor — ahora el precio arranca **vacío y se teclea siempre**. Tests **352 → 363**; **batería 13/13** con tokens de 7 roles (comercial create 200/update 403/delete 403 · los 3 mantenedores 200 · **delete 403 incluso admin** · sst/anónimo 403); **E2E de ORO de fidelidad**: corrección (85.000→95.000) + masivo (+10 %) y la cotización ENVIADA con ese ítem conservó su snapshot byte-intacto (85.000 / total 101.150) — la garantía estructural de F1 probada en vivo. Gotcha de automatización documentado: `document.querySelector('.fixed')` agarra el SIDEBAR (también `.fixed`) antes que los modales — scoping por contenido; y el InputExpresion se automatiza con TECLEO real (computer type), no con setters sintéticos.

**Coordenadas del sitio de punta a punta hasta la obra-espejo (geo-control SST)** (09-ago-2026, **PR #76**, commit de merge **`0cb9724`**, desarrollado en worktree `wt-coordenadas` SOBRE el merge del **PR #75 de SST** (`utils/geo.ts` + `getEstadoGeo` + campo `obras.coordenadas_sitio {latitud, longitud}` — shape canónico confirmado por Giovanny, NO `{lat,lng}`); cierre autónomo autorizado, **solo frontend — sin reglas, sin CF, sin dependencias**; Vercel verificado con bundle nuevo `index-liYaaW56.js` y 4 marcadores contra línea base `index-DFxwcThU.js`): el flujo comercial captura las coordenadas GPS del sitio UNA vez y viajan solas hasta la referencia que consume el geo-control de SST. **Threading** de `coordenadas_sitio` (opcional, retrocompatible) por: solicitud → cotización (formulario manual Y pipeline automático del §48 — segunda vía real detectada en build) → `construirSnapshotProyecto`/`construirSnapshotPreventivo` (spread condicional con guard) → `construirObraEspejo`. **Helpers AGREGADOS a `geo.ts` de SST** (diff verificado **+29/−0**, lección C3 — nada pisado): `esCoordenadaValida` (guard del shape, rangos ±90/±180 — rechaza `{lat,lng}` viejo), `parseCoordenadasPegadas` (par de Google Maps; coma decimal es-CO; un número suelto tipo `4,6` NO se confunde con un par — precedencia campo-individual, edge destapado en revisión del subagente), `urlVerificarEnMaps`. **Backfill completa-si-falta** en `sincronizarObraEspejo`: espejo existente sin coordenadas + snapshot con ellas → el sync las agrega; **JAMÁS reescribe** unas existentes (identidad congelada intacta — el botón "🏗 Sincronizar obra SST" es la superficie de backfill para proyectos ya en ejecución). **UI**: `CoordenadasSitioInput` (`components/sigp/shared/`, carpeta nueva) — Latitud/Longitud paste-aware, link "Verificar en Maps ↗", **Nominatim best-effort** (`/search?countrycodes=co`, AbortController 6 s, sugerencia con Usar/Descartar — nunca sobrescribe en silencio; fallo → aviso gris y la carga manual queda; el navegador no permite UA custom, a diferencia del `/reverse` de la app). Montado en el registro de solicitud (un punto FUERA del if preventivo — cubre ambos flujos) y el borrador de cotización (editable, `deleteField` al retirar); solo lectura en detalle de solicitud y ficha del proyecto (📍 Ver en Maps). Tests **363 → 382** (`geo.test.ts` NUEVO — el PR #75 llegó sin tests: Haversine con distancias verificables, `getEstadoGeo` real incl. retrocompat `sin_referencia`, guard, parser, URL — + threading en proyecto/preventivos/obraEspejo). **E2E de oro en emulador**: formulario a 1,1 km de obra sigp con coordenadas → **"⚠ Fuera de sitio" en Kanban y Lista** (78 m limpio) · obra legacy sin referencia → sin alerta · captura con paste real → SOL-2026-001 con coords → transición → el pipeline heredó al borrador de cotización · backfill por el botón real + **no-reescritura probada** (snapshot alterado a coords distintas, re-sync, obra intacta). Gotcha nuevo: el registro de solicitud llama `generarConsecutivo` — el emulador necesita **functions** incluido aunque el bloque sea "sin CF".

**Fix del validador de horario — guard de logout + ingreso en sesiones persistidas** (10-ago-2026, **PR #77**, commit de merge **`3bd5e84`**, desarrollado en worktree `wt-horario-fix`; investigación solo-lectura en prod → diseño → OK con **Opción B** → build → E2E; solo frontend — CF y reglas SIN cambios; Vercel verificado con marcadores): dos anomalías detectadas en el primer día real del módulo #3. **(1) Salida duplicada**: Giovanny generó 4 salidas en ~1,3 s — multi-clic sobre "Salir" sin feedback durante el cold start de la CF (`logout()` esperaba la CF con el botón vivo; cada clic = una invocación con token válido). Fix: **guard de en-vuelo del cierre COMPLETO** (`useRef` con promesa compartida — clics repetidos desde Header, pop-up o ambos reutilizan la misma) + `cerrandoSesion` en el contexto → botones deshabilitados con "Cerrando…". Los 3 docs duplicados se **borraron de prod con autorización** (dry-run 3/3 verificado + post-verificación; la salida real y el ingreso conservados). **(2) Ingreso en sesión persistida (el estructural)**: la colección entera tenía 5 eventos — la marca solo vivía en `login()` explícito y quien nunca re-loguea (sesión persistida de Firebase, caso Paula y todo el equipo) JAMÁS marcaba. Fix: marca movida a **`onAuthStateChanged`** (solo usuario válido tras verificación de `estado`) con guard **una-vez-por-día por uid** en localStorage (`sigp.horario.ingreso.{uid}.{YYYY-MM-DD}`, `claveDia` local; `ok` SOLO tras éxito de la CF — fallo borra la clave y el próximo arranque REINTENTA; centinela `pendiente:{ts}` TTL 2 min anti-carrera multi-pestaña — la ventana residual de ms la tolera el apareo; poda de claves de otros días). **Opción B (decisión de Giovanny)**: el login EXPLÍCITO marca SIEMPRE — banderita módulo-local levantada ANTES del `signIn` (el callback puede correr antes de que `login()` retome) y consumida exactamente una vez; el re-login del mismo día queda apareado completo y las salidas de la tarde no caen huérfanas; login rechazado por inactivo limpia la banderita. Helpers puros `claveIngresoLS`/`evaluarMarcaIngreso`/`esClaveIngresoObsoleta` en `horario.ts` (corrupto → marcar: mejor un doble tolerado que un día sin marca). Tests **382 → 400** (helpers + AuthContext con mocks de firebase: multi-clic → UNA llamada y misma promesa; persistida/F5/explícito/fallo-reintento; `cerrandoSesion`; fallo de salida no bloquea `signOut`). **E2E emulador con CF viva: 5 eventos exactos para 5 acciones** (login 1 · F5 0 · persistida 1 · 4-clics-Salir 1 · re-login B 1). Verificado de paso: heurística de dispositivo acierta (`escritorio` con Edge/Windows real). Nota operativa: el equipo con sesión persistida empieza a marcar en su PRÓXIMA recarga del panel — sin re-login; el "primer arranque del día" es la semántica (una pestaña abierta cruzando medianoche no re-marca hasta su próximo reload).

**Catálogo NEG → pestaña de Cotizaciones (reorg de navegación)** (10-ago-2026, **PR #78**, commit de merge **`4347662`**, desarrollado en worktree `wt-catalogo-tab`; reporte→propuesta→OK (con la decisión de que **Marcela ve la pestaña en solo lectura** — coherente con su Cotizaciones read-only del PR #50)→aplicar; **cosmética, cero lógica/permisos/reglas/datos nuevos** — solo Vercel): el mantenimiento de catálogo (#2b) deja de ser página propia para limpiar el sidebar SIGP. **Segmented "Cotizaciones · 📚 Catálogo NEG"** bajo la cabecera de `CotizacionesSigp` con estado en la URL (`?tab=catalogo`, replace) — sin el param la vista de Cotizaciones queda idéntica; en la pestaña de catálogo se ocultan StatCards/filtros/"Nueva cotización" y el subtítulo de la cabecera cambia. **`CatalogoPanel`** (`components/sigp/catalogo/` — git lo detectó como RENAME 90 % de `CatalogoSigp`): contenido tal cual sin cabecera de página, permisos internos intactos (`puedeMantenerCatalogoUI`). **`/sigp/catalogo` → redirect** a la pestaña (patrón `/sigp/lpus` del PR #24); ítem del sidebar fuera + su entrada en `ocultosParaGerenciaAdm` limpiada. Suite 400/400, `tsc` limpio (ningún test referenciaba la ruta). **E2E navegador**: default byte-idéntica · pestaña con mantenimiento completo (admin) · redirect viejo aterriza en la pestaña · **`?pendientes=1` verificado con montaje fresco desde otra ruta** (el flujo real del badge — un pushState in-place no re-corre el initializer del filtro, matiz de test, no de producto) · Marcela: pestaña visible, historial expandible, cero botones/checkboxes. **Gotcha de automatización nuevo**: borrar indexedDB (`deleteDatabase`) con el SDK de Firebase sosteniendo la conexión deja el delete BLOQUEADO y encola todos los `open()` posteriores — la app queda en "Cargando…" en TODAS las pestañas del origen (deadlock); remedio: cerrar todas las pestañas del origen para que el delete complete, y preferir limpiar el object store `firebaseLocalStorage` en vez de borrar la base.

**Preventivos IHS→Vertis — cliente por FLAG de dato (Ruta B)** (11-ago-2026, **PR #81**, commit de merge **`5091494`**, desarrollado en worktree `wt-vertis`; reporte→diseño en sub-bloques→OK→build→**dry-run de datos con OK explícito**→writes en prod→merge; **orden ADITIVO: datos ANTES del merge** — el bundle viejo seguía resolviendo IHS por regex, cero ventana rota): Vertis compró IHS (misma metodología) y el cliente de preventivos estaba **hardcodeado por nombre** (`/\bihs\b/` en `SolicitudForm`) — Vertis no cargaba nada. **Modelo**: `Cliente.usa_preventivos?` (patrón `usa_tipo_inversion`; checkbox en el CRUD) + `Cliente.sucede_a?` (linaje informativo SIN UI de edición — chips en el detalle del cliente con read best-effort del antecesor) + helper puro **`clientePreventivosDe()`** (primer flaggeado activo; semántica singular). **Fallback sin literales**: el form denormaliza `cliente_nombre` en la solicitud preventiva al registrar; `construirSnapshotPreventivo` cae read-vivo → denormalizado → prospecto → marcador neutro (el literal `'IHS'` eliminado). **Labels genéricos** (selector "Preventivo (precio de matriz)", candado "🔒 Preventivos", "Entregables del cliente (preventivos)", Panel "Preventivos · {mes}", etc.) con **claves de datos intactas** (`entregables_ihs`/keys no cambian). La matriz de precios sigue EN CÓDIGO (una sola metodología; si Vertis renegocia, ahí se parametriza por cliente — bloque futuro). **DATOS EN PROD** (dry-run→OK→aplicado 11-ago con post-verificación): `Vertis(5nR8pT…).usa_preventivos=true + sucede_a=IHS` · `IHS(RwPaB0…).usa_preventivos=false` (activo, historial intacto) · **LPU de IHS CLONADA** a `lpus/lpu_vertis_clon_ihs_2026` (campo `clonada_de` — NO `reemplaza_a`, que es cadena de versiones del mismo cliente; **536 ítems byte-idénticos conservando doc-ids** + Excel de 107 KB copiado en Storage con URL propia); **NIT NO tocado** (Giovanny lo corrige cuando Vertis confirme el real — ojo: hoy ambos tienen el mismo placeholder `900.123.456-8`). ⚠ **Gotcha de verificación nuevo**: comparar docs por `JSON.stringify(fields)` entre dos lecturas REST da FALSOS "distintos" (el orden de claves no está garantizado) — la post-verificación canónica (claves ordenadas en profundidad) dio **536/536 idénticos**. Tests **400 → 406** (`cliente.test.ts`: flaggeado activo gana aunque "IHS TOWERS" esté en la lista · inactivo no cuenta · ninguno → null · dos → primero · regresión del hardcode) + cadena de fallback completa. **E2E emulador con CF viva**: form resuelve VERTIS con IHS activo al lado → SOL con `cliente_nombre` → CF crea PRY `snapshot.cliente="VERTIS TORRES"` ($1.421.348 = Z3/GF/pesado exacto) → sin flaggeado → bloqueo ámbar → correctivo con la LPU de Vertis (buscador + transporte por zona). Vercel verificado con marcadores.

**#2a — Descuento global en la versión de cotización (CD y/o contra utilidad)** (11-ago-2026, **PR #82**, commit de merge **`d529ccc`**, desarrollado en worktree `wt-descuentos` por sub-bloques con el diseño de SB1 aprobado antes de codificar; solo frontend — Vercel verificado con marcadores contra línea base `index-CEuaAvqi.js` → `index-CbNY5Sk8.js`): cierra el radar #2a. **Dos modos COMBINABLES con mecánica fiscal exacta** — "sobre el total" NO existe por diseño (desalinearía la base del IVA). **Modelo**: `version.descuento? { costo_directo?, utilidad? }` (`{tipo: porcentaje|valor, valor}`) = la INTENCIÓN pactada; los **pesos CONGELADOS** viven en `TotalesCotizacion` (`descuento_cd`, `costos_directos_netos`, `descuento_utilidad`) — PDF y análisis leen SIEMPRE los pesos, jamás re-derivan del % (mismo contrato peso-a-peso de F1.4). El descuento **jamás muta ítems** (fidelidad LPU — lista intacta); `costos_directos` conserva el invariante Σ-ítems y los subtotales por grupo siguen brutos. **Cálculo** (`calcularTotales` con 6º parámetro opcional — callers sin cambio): sobre CD → ANTES del AIU → **cascada completa** (A/I/U del CD neto, el IVA baja solo); contra utilidad → `base_iva = U − desc` → el IVA baja proporcionalmente (jamás IVA sobre utilidad no cobrada); ambos → primero CD, luego utilidad sobre el resultado; `iva_pleno` ignora contra-utilidad con defensa; clamp a la base (neto jamás negativo, % a 100); **sin descuento → byte-idéntico al histórico** (test de paridad). **UI** (borrador, `puedeGestionarCotizacionesUI`): sección "Descuento" en la tarjeta de totales (toggle %/$ por modo; contra-utilidad solo en aiu; preview en vivo + aviso ámbar "Cede $X") y filas lista → −desc → neto en la tarjeta. **Nota IDEMPOTENTE** en observaciones (`observacionesConNotaDescuento` pura: la línea con el prefijo se reemplaza al recalcular y se retira al quitar — jamás duplica). "Nueva versión" copia el descuento (spread del doc, verificado). **PDF**: filas "Costo directo (lista) / Descuento comercial −$X / Costo directo neto [/ Descuento sobre utilidad −$W]" en el Resumen económico, **divisoria pre-IVA re-anclada POR NOMBRE** (no por índice), tarjeta auto-dimensionada; guion ASCII en los negativos (no se apuesta el envío al glifo U+2212); sin descuento → filas byte-idénticas. **Análisis 📊**: `utilidad real = venta − todoCosto − descuentos congelados` (fila "Descuento cedido" ámbar) — el descuento jamás infla la utilidad; con ítems sin costo la resta completa SUB-estima (conservador, decisión documentada). Tests **406 → 418**; **E2E emulador con el caso $1.000.000 AIU 10/6/8**: preview en vivo exacto ($1.147.220 con ambos modos = el caso del test), snapshot persistido (intención+congelados+nota), análisis con costo real tecleado ($312.000 = venta neta − costo), retiro limpio (campos fuera por `deleteField` + nota retirada + total de lista), y **envío con el PDF RENDERIZADO a imagen** (harness pdf2png) — filas de descuento, divisoria y nota como 4ª viñeta verificadas VISUALMENTE. Nota de verificación de bundle: los strings que se arman por concatenación en runtime (prefijo + " de $") no existen contiguos en el minificado — los marcadores deben ser los literales de las CONSTANTES.

**PDF — nombre de cliente largo envuelve a 2 líneas en los 3 generadores** (11-ago-2026, **PR #83**, commit de merge **`635e734`**, desarrollado en worktree `wt-pdf-nombre`; reporte→propuesta con renders→OK→cierre; solo frontend — sin reglas, sin CF, sin dependencias): los tres PDFs truncaban EN SILENCIO los nombres largos con `partir(valor,…)[0]` (primera línea del word-wrap, sin "…") — reporte de Giovanny con "EIC ESTRUCTURAS INTEGRALES DE…". **Cotización** (`cotizacionPdf.ts`, tarjeta CLIENTE/NIT/CONTACTO): la fila DESTACADA envuelve hasta **2 líneas por palabra** con `partirMax` (ahora exportado) + **"…" defensivo** si aun así no cabe (caso extremo ~89 chars verificado); NIT/CONTACTO se corren hacia abajo y la tarjeta crece exactamente lo de la columna más alta (interlínea 11pt); **sin desborde el trazado es byte-idéntico al histórico** (regresión por construcción, render corto verificado). **Decisión aprobada: SIN auto-reducir fuente** — el nombre es la fila protagonista y 2 líneas a 9.5pt cubren ~70 caracteres. **Preliquidación y Liquidación** (mismo patrón confirmado en su `par()` del grid CONTRATISTA/CLIENTE-SITIO): 2 líneas con interlínea 10.5 y la fila FECHA baja según la columna más alta — los nombres de contratista S.A.S. largos son el caso frecuente. Tests **418 → 425** (`cotizacionPdf.partir.test.ts` NUEVO — `partir`/`partirMax` no tenían cobertura: corte por palabra, tope de líneas, "…" que cabe en el ancho medido, sin desborde intacto; fuente mock determinística). Validado con 5 renders pdf2png (largo/extremo/corto/preli/liq). **Nota de verificación de bundle**: este diff no agrega literales de string — los marcadores fueron FIRMAS DE CÓDIGO minificado (`…9.5,F-46,2)` del `lineasDe` en el bundle principal; `-11-f*10.5` + `o-=26+(i-1)*10.5` en los chunks `preliquidacionPdf-*.js`/`liquidacionPdf-*.js` — los generadores de preli/liq viven CODE-SPLIT fuera de `index-*.js`, buscarlos ahí). Bundle `index-CbNY5Sk8.js` → `index-CowzPd5E.js`.

**MÓDULO TAREAS — asignación + "balón en cancha"** (14-ago-2026, **PR #84**, commit de merge **`0cf1b44`**, desarrollado en worktree `wt-tareas` por sub-bloques SB1 diseño→OK→motor (Fable) → SB2 UI (Sonnet, diff revisado) → E2E (Sonnet) → micro-fix → cierre autónomo autorizado con **orden ADITIVO del módulo horario: reglas FS+ST a prod ANTES del merge** — inertes sin UI; pre `prod==HEAD` byte-idéntico CRLF/LF, diff exacto **+84/−0 FS +14/−0 ST** solo bloques tareas, post byte-idéntica, 403 anónimo GET/LIST/CREATE): módulo transversal para los **9 roles del panel** tras el flag **`sigp_tareas_enabled` (default in-app FALSE** — sale oculto aunque el parámetro de Remote Config no exista; Giovanny lo crea/prende en consola). **Decisiones SB1**: asignar A OTROS = las 4 gerencias + admin (`puedeAsignarTareas()`, misma lectura de "gerencias" que veHorario); auto-tarea y pasar/recibir balón = cualquiera del panel; `hecha`/`anulada` TERMINALES sin reapertura (corrección = tarea nueva); reasignar RESETEA a pendiente y limpia el balón; el asignador JAMÁS cierra tareas ajenas. **Motor** (`types/sigp/tarea.ts`): máquina de 5 estados + **invariante `en_espera ⟺ balon_en`** + 6 patch builders PUROS (validan y devuelven el write exacto o null — la UI no improvisa writes). **Hallazgo que mató la CF del diseño**: el `read` de `users` ya cubre get+list para `esPersonalPanel()` → `listarAsignables()` es query client-side, cero cambios a reglas de `users` (caveat `cliente_final` anotado). **Reglas**: `match /tareas` con TRES vías de update — asignador · **asignado por UID del doc (primera regla del SIGP autorizada por UID, el balón lo pide)** con hasOnly de 6 campos y cierre que exige comentario + evidencia condicional · receptor del balón (solo devolverlo); sin delete; Storage `tareas/{id}/evidencia/{uuid}` auth-only H-008. **UI**: página `/tareas` (Mis tareas · 🏈 Balón en mi cancha · Equipo con deep-links `?pendientes=1`/`?balon=1`), grupo propio "Tareas" en el sidebar (entre SST y SIGP) con badge naranja clickeable, pop-up `RecordatorioTareas` una-vez-por-día por uid (patrón horario; **micro-fix del E2E**: con solo balones aterriza en `?balon=1`, no en Mis tareas vacía), hook `useTareasPendientes` (2 onSnapshot + filtro client-side — sin índices compuestos). Tests **425 → 438**; **batería de reglas 20/20** con idTokens de 5 roles (3 vías, suplantación, invariante, terminales, evidencia condicional, asignadora-no-cierra); **E2E de oro en navegador 12/12** (ciclo completo del balón GG→Paula→Marcela→cierre con timeline, upload real de evidencia vía DataTransfer, técnico frenado por el gatekeeper global; evidencia textual del DOM — el screenshot del pane falló por timeout de composición). Fricción anotada no bloqueante: el `?tab=` persiste en la URL entre logins (cae a default sin romper). Vercel `index-CowzPd5E.js` → `index-B4K_nnXh.js` con 5 marcadores literales. **Para activar**: crear el parámetro `sigp_tareas_enabled` en Remote Config → `true` → Publicar.

**Tareas SB2.2 — prerequisitos de Fase 2: contexto OC navegable + campo `activa`** (14-ago-2026, **PR #85**, commit de merge **`b65cd28`**, desarrollado en worktree `wt-tareas-prereq`; diseño→OK con 2 ajustes de Giovanny→build→batería→**deploy de reglas con OK aparte ANTES del merge** — orden OBLIGATORIO, no convención: el bundle nuevo escribe `activa` y las reglas viejas lo rechazarían): PR chico y aditivo sobre el módulo Tareas, sin generadores ni CFs. **(A) Contexto navegable**: `ContextoTarea.tipo` suma `'orden_compra'` con la convención nueva **`id` = documento ORIGEN siempre** (doc id de la OC — la cadena acción→documento origen es referencial, no textual, criterio Trinorma) **+ `ruta_id?` = override de navegación** para entidades sin ficha propia (la OC se navega por la ficha de su proyecto — C2: surfacear, no reinventar); `rutaContexto()` resuelve `ruta_id ?? id`. `'preliquidacion'` NO se agregó (sería duplicado semántico de `'proyecto'` — misma ruta; ratificado). **(B) `activa`** (`true` salvo `hecha`/`anulada`): la mantienen los **6 patch builders** vía `esTareaActiva(estadoNuevo)` (uniforme, auto-sanadora), no la UI; hooks del asignado y del balón acotados con `where('activa','==',true)` — **igualdad+igualdad se sirve fusionando índices de campo único: SIN índice compuesto, confirmado en emulador**; pestaña Equipo default solo activas con **histórico bajo demanda** (botón o auto al filtrar por terminal; el acceso de los asignadores al histórico se conserva, solo deja de descargarse siempre); `tareas` en prod tenía 0 docs (verificado con token) → sin backfill. **(C) Reglas — diff de EXACTAMENTE 3 líneas** (verificado C3): guard de create `activa == true` (ajuste 2 de Giovanny — sin él, una tarea nacida con `activa:false` sería una obligación INVISIBLE para el asignado: evidencia que engaña a un auditor) + `'activa'` en los DOS `hasOnly` (asignado 6→7, receptor 4→5); sin guard de valor en update (mapear estado→activa en reglas = complejidad desproporcionada; builders + tests). Tests **438 → 440**; **batería 16/16** (guard ×3, flujo del asignado CON `activa` en cada patch — lo que se rompería en silencio si el hasOnly no la incluyera —, receptor no-asignador acotado, regresiones, 3 consultas de doble igualdad sin índice). Deploy FS pre `prod==HEAD` byte-idéntico → post byte-idéntica (ruleset `9c3aebe4…`) → 403 anónimo; Storage sin cambios. Vercel `index-B4K_nnXh.js` → `index-B61_NtNg.js` (marcadores contra baseline local: `Cargar histórico` 0→1, `ruta_id` 0→1, `orden_compra` 3→4, `"activa"` 5→9).

**📚 LECCIÓN DE MÉTODO — pendencia de ramas (14-ago-2026)**: una rama se declara pendiente por **equivalencia de contenido contra `main`** (`git diff main...rama` vacío o no), NUNCA por `git branch --no-merged`. El mecanismo es el **squash-merge**: en un repo que mergea con squash, `--no-merged` no falla "a veces" — **falla SIEMPRE**, porque el commit de la rama jamás pasa a ser ancestro de main y toda rama squash-mergeada queda marcada como pendiente para siempre. (Origen: el Bloque 0 reportó 4 ramas SST "pendientes"; la auditoría por contenido mostró que las 4 eran obsoletas y ninguna tocaba reglas.)

**📚 LECCIÓN DE MÉTODO — tests de reglas multi-vía (14-ago-2026)**: un test de UNA vía de una regla con varias vías `||` debe usar un actor que SOLO tenga esa vía disponible. En la batería del SB2.2, "la receptora del balón no puede tocar el título" se probó primero con Marcela — que además es gerencia: su edición pasaba LEGÍTIMAMENTE por la vía asignador y el caso no probaba lo que decía probar. **Un falso positivo en tests de reglas es peor que un falso negativo: da confianza donde no la hay.** Criterio para futuras baterías: por cada vía, elegir el actor de mínimo privilegio que solo califique por esa vía (aquí: comercial como receptor).

**F2 COMPLETO (F2.1 a+b+c+d + F2.2 + F2.3 ligero) — LISTO PARA GO-LIVE.** El ciclo de Gerencia de Proyectos cubre: cotización aprobada O preventivo IHS aceptado → proyecto → asignación → permisos → preliquidación → anticipo → ejecución (con entregables IHS en preventivos) → entrega → soporte → handoff a facturación + evaluación del contratista. Siguiente: go-live (lista de abajo — reglas YA desplegadas el 20-jul; falta el parámetro Remote Config); luego avance por actividad / informe fotográfico (futuro) o el módulo de Administrativa.

**APARCADO hasta el go-live de F2** (lista CONFIRMADA al cierre de F2.2 — no ejecutar antes):
1. Crear el parámetro Remote Config **`sigp_f2_enabled`** (boolean, default `false`) en la consola — lo hace Giovanny.
2. **Deploy de la regla `proyectos`** en `firestore.rules` (read `accesoSIGP`, write `puedeGestionarProyectos`) — con OK explícito y verificación pre-deploy (prod == HEAD). Hasta ese deploy, las escrituras a `proyectos` fallarían en producción — coherente con el flag apagado.
3. **Deploy de `storage.rules`**: bloque `proyectos/**` auth-only (adjuntos de permisos de ingreso y evidencias de anticipo; 2.1.b/c).
4. **Regla Firestore de segregación (2.1.c)**: `gerencia_administrativa` puede `update` de `proyectos` SOLO sobre `preliquidacion`/`estado`/`historial`/`fecha_actualizacion` (patrón `hasOnly`, como `contratistas.estado`) — ya escrita en `firestore.rules`, va en el mismo deploy del punto 2.
5. Functions: **nada** — el prefijo `PRY` ya está en la `generarConsecutivo` desplegada desde F0.

El merge de 2.1.a a `main` **NO requiere deploy alguno**: el código queda invisible tras el flag y no toca reglas ni functions en producción.

Cada fase termina con: reglas de seguridad actualizadas, tests verdes, deploy a producción con feature flag, y un chequeo de brechas ISO cubiertas.

---

## 13. Documentos SGI del proyecto (referencia)

Estos documentos viven en el Project de Claude y son la fuente de verdad para las reglas de negocio, políticas y estructura organizacional:

- `DOCUMENTO_MAESTRO_SIGP_NEG.md` — visión estratégica del SIGP
- `POLITICA_INTEGRADA_DE_GESTION_Y_OBJETIVOS_ESTRATEGICOS.docx` — política ISO
- `CARACTERIZACION_INTEGRAL__GERENCIA_DE_PROYECTOS.xlsx` — proceso proyectos
- `CARACTERIZACION_INTEGRAL_GERENCIA__COMERCIAL.xlsx` — proceso comercial
- `MT_INDICADORES_DEL_SGI.xlsx` — indicadores que el SIGP debe alimentar
- `MT_RIESGOS_Y_OPORTUNIDADES.xlsx` — riesgos que el sistema debe mitigar
- `LISTADO_MAESTRO_DE_DOCUMENTOS_Y_CONTROL_DE_REGISTROS.xls` — catálogo documental
- `DC_ORGANIGRAMA.pdf` — estructura de roles

Ante duda sobre alcance ISO o proceso NEG, consultar estos documentos antes que la web.

---

## 14. Contacto

Este repositorio es propiedad de NEG Ingeniería S.A.S. BIC. La dirección técnica del SIGP la lleva la Gerencia con soporte del equipo de Gestión Integral. Cualquier PR que toque las reglas del capítulo 7 requiere revisión de la Gerencia.

*Última actualización de este archivo: 21 de julio de 2026 — **F2 COMPLETO** (F2.1: proyecto de la aprobación al handoff · F2.2: preventivos IHS con matriz y cliente fijo · F2.3 ligero: entregables IHS con gate en la entrega; todo tras `sigp_f2_enabled` default false). **Reglas Firestore (`proyectos` + segregación gerencia) y Storage (`proyectos/**`) YA DESPLEGADAS en producción (20-jul, prod == HEAD verificado)** — el go-live solo espera que Giovanny cree el parámetro Remote Config `sigp_f2_enabled` y decida el flip. F1 sigue EN PRODUCCIÓN con el piloto comercial activo. Pendientes: módulo Administrativa (factura/pagos/saldo con gate), ajustes del piloto, aviso de "versión nueva del panel", campo `celular`, H-008. UX: LPU en el detalle de Cliente + historial de contactos en Solicitudes (20-jul). Panel SIGP de indicadores HECHO (21-jul) y regla `indicadores` DESPLEGADA en producción (21-jul) — el registro manual del ind. 5 ya opera. PDF de cotización compactado y rebalanceado (22-jul, PR #28). Asunto canónico solicitud ↔ cotización (22-jul, PR #29). Transporte automático IHS (22-jul, PR #30). Obra-espejo SST desde el proyecto + limpieza autorizada de `obras` en prod (22-jul, PR #31). Nombre/código del sitio de punta a punta (22-jul, PR #32 — Bloque 1). "Obras" al grupo SST del sidebar (22-jul, PR #33 — Bloque 6). Tipo de inversión gateado por cliente (22-jul, PR #34 — Bloque 2). Contratista ↔ técnicos con CF de auto-asignación (22-jul, PR #35 — Bloque 3+5; CF desplegada y operando). Corrección de preliquidación ISO 7.5 (22-jul, PR #36 — Bloque 4). **TANDA DE 6 MEJORAS DEL PILOTO COMPLETA.** Módulo Gerencia Administrativa · Bloque 1: facturación (22-jul, PR #37 + regla desplegada). Hotfix A: Obras restringida + regla de creación desplegada (23-jul, PR #38). Hotfix B: corrección visible (23-jul, PR #39) + tramo de ejecución con ajuste trazable para la liquidación (23-jul, PR #40). Administrativa B2: pago del cliente (23-jul, PR #41). Administrativa 3a: gate SST con proyección `verificaciones_sst` sin datos financieros + CF `sincronizarVerificacionSst` (23-jul, PR #42). **PENDIENTE DE DEPLOY (con OK aparte): reglas Firestore (pago_cliente + 3a, un solo deploy cubre ambos) + la CF `sincronizarVerificacionSst` + backfill (touch del proyecto real en `facturado`)** — hasta ese deploy, en producción la cola SST y el chip del gate fallan en silencio (reglas de `verificaciones_sst` ausentes → denegado por catch-all). Administrativa 3b: liquidación (23-jul, PR #43 + regla desplegada) — **corazón del módulo administrativo COMPLETO: factura → pago → gate SST → liquidación**. Cierre + bandeja "Gestión Administrativa" 7 secciones (23-jul, PR #44 + regla desplegada) — **CICLO COMPLETO de punta a punta: solicitud → … → liquidación → cierre; cerrado inmutable en reglas**. Aprobador de respaldo con salvedad (23-jul, PR #45 + regla desplegada — la aprobación de preliquidación queda blindada por-campo en reglas). Liquidación anticipada (23-jul, PR #46 + regla desplegada) — **TANDA DE 3 COMPLETA (bandeja 7 secciones → aprobador de respaldo → anticipada); la máquina hacia liquidado quedó acotada a orígenes exactos**. Cotizador INGEMEC Matriz vs. NEG con factor 72%/15% por ítem (23-jul, PR #47; `esquema_factor` habilitado en el LPU real de prod por write puntual autorizado). Pipeline automático solicitud → visita → cotización con borradores sin código + badges clickeables (23-jul, PR #48 — el consecutivo se asigna al materializar, contigüidad ISO). Columna "Nombre del sitio" + búsqueda en bandejas de Solicitudes y Visitas (23-jul, PR #49). Panel de Marcela: sidebar enfocado + mapa proactivo "En camino" con narrativa (23-jul, PR #50). Saludo personalizado en Gestión Administrativa (23-jul, PR #51). Modalidad de contratación + indicador ISO #3 honesto (24-jul, PR #52 — costo/presupuesto por canasta; pendiente externo: Caracterización v02 con aval de Ingrid/GI). Importador de LPU: detección "UN" + guardia bloqueante con forzar consciente (27-jul, PR #53 — pendiente aparte: backfill de los 153 ítems 4.x del LPU INGEMEC en prod, "Prompt C"). Regla unificada solicitud→«cotizada» al enviar + reparación SOL-2026-016 en prod (27-jul, PR #54). Reparación de datos autorizada en prod: 153 unidades del LPU INGEMEC + renumeración 3.5.18→3.5.19 + 6 unidades del snapshot COT-2026-017 — PDF del contratista de PRY-2026-002 completo (27-jul, sin PR; oráculo del Excel original, dry-run aprobado). Selector de orden en la bandeja administrativa (27-jul, PR #56). Solicitud → «aceptada · proyecto creado» al aprobar (27-jul, PR #55 — mergeado finalmente vía gh con autorización, tras dos cierres accidentales sin merge; las 3 solicitudes históricas de prod reparadas aparte). PRs #57–#60 de sesiones paralelas (SST + reglas users; reglas desplegadas 30-jul — bitácora detallada pendiente de esas sesiones). **MÓDULO COMPRAS C1: proveedores** (02-ago, PR #61 + reglas Firestore/Storage desplegadas 02-ago) — siguiente fase: C2 (órdenes de compra) + wizard manual de `contactos_lookup`. Codificación ISO en el PDF de preliquidación (02-ago, PR #62 — SGI-FT-PRL-26 v01; pendiente externo: ratificación de Ingrid + Listado Maestro, frente Trinorma). **MÓDULO COMPRAS C2: órdenes de compra** (02-ago, PR #63 + CF prefijo OC + reglas desplegadas 02-ago). **MÓDULO COMPRAS C3: compra + auto-agregado al indicador** (03-ago, PR #64 + fix #65 — reglas C3 pisadas por sesión paralela, restauradas; 2 CFs + reglas/Storage desplegadas 03-ago; el hardening de formularios/revision_sst de la sesión SST quedó en main y desplegado — esa sesión debe validarlo). **MÓDULO COMPRAS COMPLETO C1+C2+C3+C4** (C4: conciliación de reembolsos, 03-ago, PR #66 — canasta del indicador completa: mano de obra + OCs + menores + reembolsos): proveedor → OC → aprobación → compra → indicador automático conciliado. En el radar: **hallazgo §16** (comercial lee proyectos completo incl. preliquidación — fisura preexistente de F2; endurecimiento con sub-doc confidencial, revisar con Giovanny/Ingrid), Compras v2 (recepción/devolución), wizard de `contactos_lookup`, `residente_obra` a los helpers OC, retenciones según la reunión del área; Caracterización v02 (gate del #3) con Ingrid. Comercial #1: decisión de rumbo al registrar la solicitud (07-ago, PR #68) + **práctica de worktrees por carril adoptada** (cap. 10 — un worktree por sesión activa, el principal reservado a main). Indicador operativo de margen real con meta configurable (07-ago, PR #69 — primer bloque 100% en worktree; regla `configuracion` desplegada 07-ago; mecánica de worktree documentada completa: deps raíz+functions, copiar .env/.env.local, emulador/vite desde el worktree, matar procesos antes del remove). **§16 (ii) RESUELTO: nacimiento del proyecto server-side (CF `crearProyectoAlAprobar` con staging de `snapshot_proyecto`) + comercial FUERA de `proyectos` en reglas y UI** (07-ago, PR #70; deploy contractual CF → Vercel → reglas ejecutado y verificado — incl. el fix del `delete` que atrapó la batería; el hallazgo §16 del radar queda CERRADO). Codificación ISO en el PDF de liquidación — SGI-FT-LIQ-26 (07-ago, PR #71; los 3 documentos entregables con control documental; el "PDF de visitas" NO existe — informe de visita queda como bloque futuro opcional; pendiente externo: Ingrid ratifica LIQ-26 + PRL-26 en el Listado Maestro). **MÓDULO #3 — Validador de horario/asistencia** (09-ago, PR #72; CF `registrarEventoHorario` + `registros_horario` solo-CF + `ausentismos` con detalle médico de línea dura + bandeja `/administrativa/horario` + pop-up de fin de jornada para todos; reglas FS/ST + CF desplegadas ANTES del merge — orden aditivo; match `configuracion/horario` read isAuthed ratificado; Identity Platform descartado salvo que un día se quiera BLOQUEAR). **#2b — Mantenimiento de precios del catálogo NEG** (09-ago, PR #73; `historial_precios[]` trazable con siembra desde el cotizador + página `/sigp/catalogo` con corrección y ajuste masivo + regla `puedeMantenerCatalogo` (GG/GP/admin) SIN delete desplegada al final orden §16; fix del prefill del precio; E2E de oro de fidelidad verificado en vivo). **Coordenadas del sitio → obra-espejo (geo-control SST)** (09-ago, PR #76 SOBRE el PR #75 de SST — shape canónico `{latitud, longitud}` confirmado; helpers agregados a `geo.ts` +29/−0 sin pisar lo de SST; threading solicitud→cotización→snapshot→obra con guard; backfill completa-si-falta vía "Sincronizar obra SST" sin reescribir jamás; `CoordenadasSitioInput` paste-aware + Nominatim best-effort; `geo.test.ts` nuevo — el #75 llegó sin tests; solo frontend, Vercel verificado). **Fix del validador de horario** (10-ago, PR #77 — guard de en-vuelo en `logout()` + "Cerrando…" contra el multi-clic (4 salidas duplicadas de prod, borradas con autorización) + **marca de ingreso movida a `onAuthStateChanged`** con guard una-vez-por-día en localStorage y Opción B (login explícito marca siempre) — las sesiones persistidas por fin marcan (antes de esto la colección tenía 5 eventos: nadie sin re-login marcaba jamás); tests 400, E2E 5-eventos-por-5-acciones). **Catálogo NEG → pestaña de Cotizaciones** (10-ago, PR #78 — reorg de navegación cosmética: segmented `?tab=catalogo` en `CotizacionesSigp` + `CatalogoPanel` (rename 90 % de la página vieja) + redirect de `/sigp/catalogo` + ítem fuera del sidebar; Marcela ve la pestaña en solo lectura por decisión ratificada; cero cambio de permisos/reglas/datos; suite 400/400 + E2E de navegación completo con `?pendientes=1` intacto). **Preventivos IHS→Vertis por flag de cliente — Ruta B** (11-ago, PR #81 — `usa_preventivos` + `sucede_a` + `clientePreventivosDe()` reemplazan el hardcode `\bihs\b`; `cliente_nombre` denormalizado mata el literal del snapshot; labels genéricos con claves de datos intactas; **datos en prod aplicados ANTES del merge con dry-run**: flags Vertis/IHS + LPU clonada 536/536 canónico + Excel copiado; NIT pendiente de Giovanny cuando Vertis confirme el real — ambos clientes hoy con el mismo placeholder). **#2a — Descuento global en la versión de cotización RESUELTO** (11-ago, PR #82 — dos modos combinables CD y/o contra-utilidad con mecánica fiscal exacta, sin "sobre el total"; intención en `version.descuento` + pesos congelados en `totales`; cascada del AIU sobre el CD neto e IVA sobre la utilidad neta; nota idempotente en observaciones; PDF con filas nuevas y divisoria por nombre; análisis 📊 con utilidad neta; tests 418 + E2E con PDF renderizado — el radar del par queda VACÍO). **PDF — nombre de cliente largo a 2 líneas en los 3 generadores** (11-ago, PR #83 — `partirMax` exportado + "…" defensivo en la tarjeta CLIENTE de cotización (la tarjeta crece, NIT/CONTACTO bajan; sin desborde → byte-idéntico; sin auto-reducir fuente) y en el grid CONTRATISTA/CLIENTE-SITIO de preliquidación y liquidación; antes `partir(...)[0]` truncaba en silencio; tests 425 con cobertura nueva de `partir`/`partirMax`; gotcha de verificación: preli/liq viven en chunks code-split `preliquidacionPdf-*.js`/`liquidacionPdf-*.js`, no en `index-*.js`). **MÓDULO TAREAS: asignación + balón en cancha** (14-ago, PR #84 — 9 roles del panel tras `sigp_tareas_enabled` default false; máquina de 5 estados con terminales duros + invariante del balón + patch builders puros; reglas con 3 vías incl. la primera autorizada por UID del doc, desplegadas ANTES del merge en orden aditivo con pre/post byte-idéntica y 403 anónimo; `listarAsignables()` client-side — CF descartada, `users` intacto; página `/tareas` + badge + pop-up con micro-fix `?balon=1`; batería 20/20 + E2E de oro 12/12; tests 438; para activar: crear el parámetro en Remote Config). **Tareas SB2.2 — prerequisitos de F2** (14-ago, PR #85 — `'orden_compra'` en contexto con convención `id`=origen + `ruta_id`=navegación (la OC se navega por la ficha del proyecto); campo `activa` mantenido por los 6 builders, hooks y Equipo acotados sin índice compuesto, histórico bajo demanda; reglas +3 líneas exactas (guard de create `activa==true` + 'activa' en los 2 hasOnly) desplegadas ANTES del merge — orden obligatorio, el bundle nuevo escribe `activa`; tests 440, batería 16/16; **2 lecciones de método nuevas**: pendencia de ramas por equivalencia de contenido — `--no-merged` falla SIEMPRE con squash-merge — y tests de reglas multi-vía con actor de mínimo privilegio por vía). Bitácora consolidada la hace Giovanny. Versión: 2.5.39.*
