node_modules/
dist/
.env
.env.local
.vercel/
*.log

# Credenciales / service accounts — NUNCA subir al repo
*firebase-adminsdk*.json
*serviceAccount*.json
*service-account*.json
*.key.json

# Salidas de scripts de auditoría
functions/audit-*.csv

# Exports del Firebase Emulator Suite (datos locales de prueba)
emu-export/
firebase-export-*/

# Brainstorming companion (visual mockups)
.superpowers/

# Datos comerciales de licitaciones (fuentes de importación) — precios y
# procesos reales. NUNCA se commitean.
_datos/
