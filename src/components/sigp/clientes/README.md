# Componentes SIGP · Clientes

CRUD de clientes SIGP y visualización de sus datos comerciales.

Entidades: `clientes` (con campos `tiene_lpu` y `tipo_documento_cierre`).

Se agrega en: Fase 1 (Comercial).
