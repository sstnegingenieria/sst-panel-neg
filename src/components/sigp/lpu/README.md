# Componentes SIGP · LPU

Carga desde Excel, parser, preview y visualización de las Listas de
Precios Unitarios de cada cliente.

Entidades: `lpu_clientes` (con `items` snapshot y `archivo_url`).

Se agrega en: Fase 1 (Comercial).
